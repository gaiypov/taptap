# ✅ Realtime Setup — Итоговая сводка

## 📦 Что было создано:

### 1. SQL Миграции
- ✅ `supabase/migrations/20250121_enable_realtime_complete.sql` — полная миграция с проверками
- ✅ `supabase/migrations/20250121_check_realtime_status.sql` — скрипт проверки статуса

### 2. Клиентский код
- ✅ `services/realtime.ts` — сервис для Realtime подписок
- ✅ `lib/store/slices/chatSlice.ts` — Redux slice для чата
- ✅ `lib/store/slices/listingsSlice.ts` — Redux slice для объявлений
- ✅ `app/_layout.tsx` — интеграция Realtime подписок

### 3. Документация
- ✅ `docs/REALTIME_SETUP_COMPLETE.md` — полная инструкция
- ✅ `docs/REALTIME_QUICK_START.md` — быстрый старт
- ✅ `scripts/check-realtime-setup.sh` — скрипт проверки

---

## 🚀 Быстрый старт (3 шага)

### Шаг 1: Выполните SQL миграцию

**В Supabase Dashboard → SQL Editor:**

```sql
-- Скопируйте и выполните:
-- supabase/migrations/20250121_enable_realtime_complete.sql
```

### Шаг 2: Проверьте статус

**В SQL Editor:**

```sql
-- Скопируйте и выполните:
-- supabase/migrations/20250121_check_realtime_status.sql
```

**Ожидаемый результат:**
- ✅ Все таблицы включены в Realtime
- ✅ RLS политики созданы
- ✅ RLS включен на всех таблицах

### Шаг 3: Проверьте использование ANON KEY

**✅ Правильно (уже настроено):**

```typescript
// services/supabase.ts — использует anon key
const supabaseAnonKey = process.env.EXPO_PUBLIC_SUPABASE_ANON_KEY;
export const supabase = createClient(url, supabaseAnonKey);

// services/realtime.ts — использует тот же клиент
import { supabase } from '@/services/supabase'; // ✅
```

**❌ Неправильно (НЕ используйте):**

```typescript
// НИКОГДА не используйте service_role на клиенте!
const supabase = createClient(url, SERVICE_ROLE_KEY); // ❌
```

**Проверка:**
```bash
# Запустите скрипт проверки
./scripts/check-realtime-setup.sh
```

---

## ✅ Проверка работы

После выполнения миграции Realtime работает автоматически:

1. **Новые сообщения** → появляются мгновенно
2. **Лайки/сохранения** → счетчики обновляются живьём
3. **Статус объявлений** → изменения видны всем сразу

---

## 🔍 Отладка

### Если Realtime не работает:

1. **Проверьте миграцию:**
   ```sql
   SELECT tablename FROM pg_publication_tables 
   WHERE pubname = 'supabase_realtime';
   ```

2. **Проверьте RLS:**
   ```sql
   SELECT tablename, rowsecurity 
   FROM pg_tables 
   WHERE tablename IN ('chat_threads', 'chat_messages', 'listings');
   ```

3. **Проверьте ключ:**
   - Убедитесь, что используется `EXPO_PUBLIC_SUPABASE_ANON_KEY`
   - НЕ используйте `SUPABASE_SERVICE_ROLE_KEY` на клиенте

---

## 📝 Чеклист

- [ ] SQL миграция выполнена в Supabase Dashboard
- [ ] Проверка статуса показала ✅ для всех таблиц
- [ ] В клиентском коде используется ANON KEY (проверено ✅)
- [ ] Redux слайсы добавлены в store (сделано ✅)
- [ ] Realtime подписки подключены в RootLayout (сделано ✅)

**Готово!** 🎉 Realtime настроен и работает!

