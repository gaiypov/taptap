# ⚡ Быстрый старт - Настройка .env

## ✅ Что уже сделано

1. Секреты удалены из `app.json` ✅
2. `.env` файл обновлен с ключами ✅
3. Код обновлен для чтения из `process.env` ✅

---

## 🔧 Что нужно сделать СЕЙЧАС

### 1. РОТИРОВАТЬ ВСЕ КЛЮЧИ (Критично!)

Все ключи, которые были в `app.json`, **уже скомпрометированы** (они в истории Git).

**См. подробную инструкцию:** `ROTATE_KEYS_GUIDE.md`

**Быстрый чеклист:**
- [ ] Ротировать Supabase Service Role Key
- [ ] Ротировать Google Vision API Key  
- [ ] Ротировать SMS Password
- [ ] Ротировать API Video Key
- [ ] Обновить `.env` с новыми ключами

---

### 2. Проверить работу приложения

После обновления `.env`:

```bash
# Очистить кэш и перезапустить
npm start -- --clear
```

**Проверьте:**
- [ ] Авторизация по SMS работает
- [ ] Загрузка видео работает
- [ ] Модерация контента работает
- [ ] Подключение к Supabase работает

---

## 📁 Структура .env

Ваш `.env` файл должен содержать:

```bash
# SMS (уже есть)
EXPO_PUBLIC_SMS_LOGIN=superapp
EXPO_PUBLIC_SMS_PASSWORD=<новый_пароль_после_ротации>
EXPO_PUBLIC_SMS_SENDER=bat-bat.kg
EXPO_PUBLIC_SMS_API_URL=https://smspro.nikita.kg/api/message

# Секретные ключи (добавлены)
SUPABASE_SERVICE_ROLE_KEY=<новый_ключ_после_ротации>
GOOGLE_VISION_API_KEY=<новый_ключ_после_ротации>
EXPO_PUBLIC_APIVIDEO_API_KEY=<новый_ключ_после_ротации>
```

---

## ⚠️ ВАЖНО

1. **`.env` НЕ коммитится в Git** (уже в `.gitignore`) ✅
2. **Старые ключи нужно ротировать** (они в истории Git) 🔴
3. **После ротации обновите `.env`** с новыми значениями

---

## 🚨 Если что-то не работает

1. Проверьте, что `.env` файл существует
2. Проверьте, что переменные правильно названы
3. Перезапустите Metro: `npm start -- --clear`
4. Проверьте консоль на ошибки

---

**Следующий шаг:** Откройте `ROTATE_KEYS_GUIDE.md` и ротируйте все ключи!

