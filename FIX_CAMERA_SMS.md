# Исправление проблем с камерой и SMS

## ✅ Что исправлено

### 1. Камера (запись видео)
- ✅ Добавлена автоматическая установка `cameraReady` через 1 секунду после получения разрешений
- ✅ Добавлен обработчик ошибок `onMountError` для диагностики проблем с камерой
- ✅ Улучшена обработка готовности камеры в `app/camera/record.tsx` и `app/camera.tsx`
- ✅ Добавлены логи для отладки

### 2. SMS (отправка кодов)
Backend уже настроен на использование реального SMS сервиса (nikita.kg).

**Важно:** Убедитесь, что в `.env` файле backend установлены правильные credentials:

```env
SMS_LOGIN=superapp
SMS_PASSWORD=ваш_реальный_пароль
SMS_API_URL=https://smspro.nikita.kg/api/message
SMS_SENDER=bat-bat.kg
```

## 🔧 Что нужно проверить

### Для SMS:
1. Проверьте файл `.env` в папке `backend/` - там должны быть реальные credentials
2. Убедитесь, что `EXPO_PUBLIC_USE_MOCK=false` в `app.json` (уже установлено)
3. Перезапустите backend сервер после изменения `.env`

### Для камеры:
1. Убедитесь, что приложение запросило разрешения на камеру
2. Проверьте логи в консоли - должны быть сообщения "✅ Camera is ready"
3. Если камера не готова, подождите 1-2 секунды после открытия экрана камеры

## 📝 Проверка работы

### SMS:
1. Откройте экран авторизации
2. Введите номер телефона
3. Нажмите "Отправить код"
4. Проверьте логи backend - должно быть сообщение об отправке SMS
5. Если SMS не приходит, проверьте credentials в `.env`

### Камера:
1. Откройте экран записи видео
2. Дождитесь сообщения "✅ Camera is ready" в консоли
3. Нажмите кнопку записи
4. Если появляется ошибка "Камера не готова", подождите еще 1-2 секунды

## 🚨 Если проблемы остаются

### SMS не работает:
1. Проверьте логи backend: `console.log` в `backend/services/smsService.ts`
2. Убедитесь, что `SMS_PASSWORD` установлен в `.env` backend
3. Проверьте, что backend сервер запущен и доступен
4. Проверьте баланс на аккаунте nikita.kg

### Камера не записывает:
1. Проверьте разрешения в настройках устройства
2. Перезапустите приложение
3. Проверьте логи в консоли на наличие ошибок
4. Убедитесь, что используется реальное устройство (не эмулятор)

