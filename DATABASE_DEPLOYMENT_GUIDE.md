# 🚀 Руководство по Развертыванию Базы Данных 360Auto

## 📋 Подготовка

### Что вам понадобится:
1. ✅ Аккаунт Supabase (бесплатный tier подойдет для MVP)
2. ✅ Доступ к SQL Editor в Supabase Dashboard
3. ✅ 10-15 минут свободного времени

## 📝 Пошаговая Инструкция

### Шаг 1: Откройте Supabase Dashboard

1. Перейдите на https://app.supabase.com
2. Войдите в свой аккаунт
3. Выберите ваш проект **360Auto** (или создайте новый)

### Шаг 2: Откройте SQL Editor

1. В левом меню найдите **SQL Editor**
2. Нажмите **New Query**

### Шаг 3: Выполните Схему БД

1. Откройте файл `supabase-complete-schema.sql` из корня проекта
2. Скопируйте **весь** контент файла
3. Вставьте в SQL Editor
4. Нажмите **Run** (или Ctrl/Cmd + Enter)

⏱️ **Время выполнения:** ~30-60 секунд

### Шаг 4: Проверьте Результат

После успешного выполнения вы должны увидеть:

```
Success. No rows returned
```

#### Проверка таблиц:
1. Перейдите в **Table Editor** (левое меню)
2. Вы должны увидеть следующие таблицы:
   - ✅ users
   - ✅ cars
   - ✅ likes
   - ✅ saves
   - ✅ conversations
   - ✅ messages
   - ✅ verification_codes
   - ✅ activity_logs
   - ✅ views
   - ✅ notifications
   - ✅ subscriptions
   - ✅ payments

#### Проверка Storage:
1. Перейдите в **Storage** (левое меню)
2. Вы должны увидеть buckets:
   - ✅ car-videos
   - ✅ car-thumbnails
   - ✅ avatars
   - ✅ documents

### Шаг 5: Проверьте RLS Policies

1. Выберите любую таблицу (например, `cars`)
2. Перейдите на вкладку **Policies**
3. Вы должны увидеть несколько политик RLS
4. Проверьте что все политики **enabled** (зеленая галочка)

## ✅ Проверка Работоспособности

### Тест 1: Создание Пользователя

Выполните в SQL Editor:

```sql
INSERT INTO users (phone, name)
VALUES ('+996555123456', 'Test User')
RETURNING *;
```

**Ожидаемый результат:** Одна строка с созданным пользователем

### Тест 2: Создание Автомобиля

```sql
-- Сначала получите ID пользователя из предыдущего теста
INSERT INTO cars (
  seller_id,
  brand,
  model,
  year,
  price,
  mileage,
  location,
  video_url,
  thumbnail_url
) VALUES (
  'YOUR_USER_ID', -- замените на реальный ID
  'Toyota',
  'Camry',
  2020,
  1500000,
  50000,
  'Бишкек',
  'https://example.com/video.mp4',
  'https://example.com/thumb.jpg'
) RETURNING *;
```

**Ожидаемый результат:** Одна строка с созданным автомобилем

### Тест 3: Функции

```sql
-- Тест increment views
SELECT increment_views('YOUR_CAR_ID');

-- Проверка
SELECT views FROM cars WHERE id = 'YOUR_CAR_ID';
```

**Ожидаемый результат:** views должно увеличиться на 1

## 🔒 Безопасность

### Проверка RLS

1. Откройте **Authentication** → **Policies** в Supabase
2. Убедитесь, что RLS включен для всех таблиц
3. Протестируйте доступ от анонимного пользователя

### Настройка API Keys

Ваши ключи находятся в:
**Settings** → **API**

Нужны два ключа:
1. **anon public** - для клиента (уже в `app.json`)
2. **service_role** - НЕ публиковать! (для backend)

## 📊 Мониторинг

### Включите Monitoring

1. Перейдите в **Reports** (левое меню)
2. Включите:
   - ✅ API requests monitoring
   - ✅ Database performance
   - ✅ Storage usage

### Настройте Alerts

1. **Settings** → **Alerts**
2. Добавьте алерты для:
   - Database size > 80%
   - API rate limit > 80%
   - Storage > 80%

## 🔧 Troubleshooting

### Проблема: "Permission denied for schema public"

**Решение:**
```sql
GRANT USAGE ON SCHEMA public TO anon, authenticated;
GRANT ALL ON ALL TABLES IN SCHEMA public TO anon, authenticated;
```

### Проблема: "relation already exists"

**Решение:** Таблица уже создана. Можно:
1. Пропустить (если все работает)
2. Удалить таблицу и создать заново:
```sql
DROP TABLE IF EXISTS table_name CASCADE;
```

### Проблема: RLS блокирует запросы

**Решение:**
1. Проверьте что пользователь авторизован
2. Проверьте политики RLS
3. Временно отключите RLS для тестирования:
```sql
ALTER TABLE table_name DISABLE ROW LEVEL SECURITY;
```

⚠️ **НЕ ЗАБУДЬТЕ ВКЛЮЧИТЬ ОБРАТНО!**

## 📈 Оптимизация

### После Развертывания

1. **Создайте индексы** (уже в схеме) ✅
2. **Настройте Connection Pooling:**
   - Settings → Database → Connection pooling
   - Рекомендуется для production

3. **Включите Point in Time Recovery:**
   - Settings → Database → Backups
   - Для защиты от потери данных

4. **Настройте Automatic Backups:**
   - Включено по умолчанию на платных планах
   - Для бесплатного: делайте ручные backups

## 🧪 Тестовые Данные

### Добавить Тестовые Данные

Создайте файл `seed.sql`:

```sql
-- Пользователи
INSERT INTO users (phone, name, is_verified) VALUES
('+996555000001', 'Айбек Жумабаев', true),
('+996555000002', 'Гульнара Токтогулова', true),
('+996555000003', 'Эрлан Касымов', false);

-- Автомобили
INSERT INTO cars (seller_id, brand, model, year, price, mileage, location, video_url, thumbnail_url, status) 
SELECT 
  id,
  'Toyota',
  'Camry',
  2020,
  1500000,
  50000,
  'Бишкек',
  'https://example.com/video1.mp4',
  'https://example.com/thumb1.jpg',
  'active'
FROM users LIMIT 1;
```

Выполните в SQL Editor.

## 📱 Подключение из Приложения

Ваше приложение уже настроено! Проверьте `app.json`:

```json
{
  "extra": {
    "EXPO_PUBLIC_SUPABASE_URL": "https://YOUR_PROJECT.supabase.co",
    "EXPO_PUBLIC_SUPABASE_ANON_KEY": "YOUR_ANON_KEY"
  }
}
```

## ✨ Следующие Шаги

После успешного развертывания:

1. ✅ Протестируйте авторизацию в приложении
2. ✅ Загрузите тестовое видео автомобиля
3. ✅ Проверьте работу лайков и сохранений
4. ✅ Протестируйте чат
5. ✅ Настройте real-time subscriptions

## 📞 Поддержка

### Если что-то не работает:

1. **Проверьте логи:**
   - Supabase Dashboard → Logs
   
2. **Проверьте API docs:**
   - Supabase Dashboard → API Docs
   
3. **Документация Supabase:**
   - https://supabase.com/docs

4. **Community:**
   - https://github.com/supabase/supabase/discussions

## 🎉 Готово!

Теперь ваша база данных готова к использованию!

### Чек-лист Финальной Проверки:

- [ ] Все таблицы созданы
- [ ] RLS policies активны
- [ ] Storage buckets созданы
- [ ] Индексы работают
- [ ] Функции работают
- [ ] Тестовые данные добавлены
- [ ] Приложение подключено
- [ ] Monitoring настроен
- [ ] Backups настроены

---

**Создано:** 11 октября 2025  
**Версия:** 1.0  
**Для проекта:** 360Auto MVP

