# Настройка Supabase для 360Auto MVP

## 1. Создание проекта Supabase

1. Перейдите на [supabase.com](https://supabase.com)
2. Нажмите "Start your project"
3. Войдите через GitHub или создайте аккаунт
4. Нажмите "New Project"
5. Выберите организацию и введите:
   - **Name**: `360Auto MVP`
   - **Database Password**: создайте надежный пароль
   - **Region**: выберите ближайший регион
6. Нажмите "Create new project"

## 2. Получение ключей

После создания проекта:

1. Перейдите в **Settings** → **API**
2. Скопируйте:
   - **Project URL** (например: `https://abcdefghijklmnop.supabase.co`)
   - **anon public** ключ (начинается с `eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...`)

## 3. Обновление конфигурации

### Вариант A: Через app.json (рекомендуется для разработки)

Замените в `app.json`:

```json
{
  "expo": {
    "extra": {
      "EXPO_PUBLIC_SUPABASE_URL": "https://ВАШ-ПРОЕКТ-ID.supabase.co",
      "EXPO_PUBLIC_SUPABASE_ANON_KEY": "ВАШ-ANON-КЛЮЧ"
    }
  }
}
```

### Вариант B: Через .env файл (для продакшена)

Создайте файл `.env` в корне проекта:

```bash
# Supabase Configuration
EXPO_PUBLIC_SUPABASE_URL=https://ВАШ-ПРОЕКТ-ID.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=ВАШ-ANON-КЛЮЧ
```

## 4. Настройка базы данных

1. Перейдите в **SQL Editor** в Supabase Dashboard
2. Скопируйте и выполните SQL из файла `supabase-schema.sql`
3. Проверьте создание таблиц в **Table Editor**

## 5. Настройка Storage

1. Перейдите в **Storage** в Supabase Dashboard
2. Создайте buckets:
   - `car-videos` (для видео автомобилей)
   - `car-thumbnails` (для превью изображений)
3. Настройте политики доступа (они уже включены в SQL схему)

## 6. Настройка RLS (Row Level Security)

Политики безопасности уже настроены в SQL схеме, но убедитесь что:

1. **RLS включен** для всех таблиц
2. **Политики активны** для:
   - `users` - пользователи видят только свои данные
   - `cars` - все видят автомобили, владельцы могут редактировать
   - `likes`, `saves` - пользователи управляют только своими
   - `conversations`, `messages` - участники чата видят только свои сообщения

## 7. Тестирование

1. Перезапустите Expo: `npx expo start --port 8082 --clear`
2. Откройте приложение в Expo Go
3. Нажмите "🧪 Test Supabase" на главном экране
4. Проверьте результаты тестов

## 8. Примеры реальных ключей

```json
{
  "EXPO_PUBLIC_SUPABASE_URL": "https://abcdefghijklmnop.supabase.co",
  "EXPO_PUBLIC_SUPABASE_ANON_KEY": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImFiY2RlZmdoaWprbG1ub3AiLCJyb2xlIjoiYW5vbiIsImlhdCI6MTY5Njg5NjAwMCwiZXhwIjoyMDEyNDcyMDAwfQ.example-signature"
}
```

## 9. Безопасность

⚠️ **Важно**:
- Никогда не коммитьте реальные ключи в Git
- Используйте `.env` файл для продакшена
- Добавьте `.env` в `.gitignore`
- Для продакшена используйте переменные окружения

## 10. Troubleshooting

### Ошибка "Missing Supabase credentials"
- Проверьте правильность ключей в `app.json`
- Убедитесь что ключи не содержат лишних пробелов
- Перезапустите Expo после изменений

### Ошибка "Invalid API key"
- Проверьте что используете `anon` ключ, а не `service_role`
- Убедитесь что проект активен в Supabase Dashboard

### Ошибка подключения к БД
- Проверьте что таблицы созданы (выполните SQL схему)
- Убедитесь что RLS политики настроены правильно
