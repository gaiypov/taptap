# 🚀 BOOST Монетизация - Полное руководство

## 📋 Оглавление

1. [Обзор системы](#обзор-системы)
2. [Тарифные планы](#тарифные-планы)
3. [Платежные системы](#платежные-системы)
4. [База данных](#база-данных)
5. [Архитектура](#архитектура)
6. [Интеграция](#интеграция)
7. [API Endpoints](#api-endpoints)
8. [Настройка](#настройка)
9. [Тестирование](#тестирование)
10. [Прогноз доходов](#прогноз-доходов)

---

## 🎯 Обзор системы

BOOST - это система монетизации для приложения 360Auto, которая позволяет продавцам автомобилей продвигать свои объявления для получения большего количества просмотров.

### Ключевые преимущества:

- ✅ **Простая интеграция** - готовые компоненты и API
- ✅ **Безопасные платежи** - интеграция с 4 местными банками
- ✅ **Прозрачная аналитика** - отслеживание эффективности
- ✅ **Автоматизация** - webhook'и и cron jobs
- ✅ **Масштабируемость** - поддержка тысяч транзакций

---

## 💰 Тарифные планы

### ⭐ Выделение (Basic)
- **Цена:** 50 сом
- **Длительность:** 24 часа
- **Эффект:** ×2 просмотры
- **Особенности:**
  - Оранжевая рамка вокруг объявления
  - Приоритет в результатах поиска
  - Значок "Выделено"

### 🔥 ТОП (Top)
- **Цена:** 150 сом
- **Длительность:** 72 часа (3 дня)
- **Эффект:** ×5 просмотров
- **Особенности:**
  - Красная рамка с градиентом
  - ТОП позиция в результатах
  - Значок "Горячее"
  - Показ на главной странице

### 💎 Премиум (Premium)
- **Цена:** 300 сом
- **Длительность:** 168 часов (7 дней)
- **Эффект:** ×10 просмотров
- **Особенности:**
  - Золотая рамка с анимацией
  - Закреплено в топе результатов
  - Все значки и бейджи
  - Промо-блок на главной
  - Email рассылка подписчикам
  - Приоритет в уведомлениях

---

## 💳 Платежные системы

### 1. Mbank
**Тип:** Банковские карты (Visa, MasterCard, Элкарт)

```typescript
// Конфигурация
MBANK_API_URL=https://api.mbank.kg
MBANK_API_KEY=your_api_key_here
MBANK_MERCHANT_ID=your_merchant_id
```

**Процесс оплаты:**
1. Создание платежа через API
2. Редирект на страницу оплаты
3. Webhook от Mbank при успешной оплате
4. Автоматическая активация BOOST

### 2. Bakai Bank (Balance)
**Тип:** Электронный кошелек Balance

```typescript
// Конфигурация
BAKAI_API_URL=https://balance.kg/api
BAKAI_MERCHANT_ID=your_merchant_id
BAKAI_SECRET_KEY=your_secret_key
```

**Преимущества:**
- Мгновенная оплата
- Популярен в Кыргызстане
- Низкие комиссии

### 3. O!Bank
**Тип:** Онлайн банкинг

```typescript
// Конфигурация
OBANK_API_URL=https://api.obank.kg
OBANK_API_KEY=your_api_key
OBANK_MERCHANT_ID=your_merchant_id
```

**Особенности:**
- Прямая оплата с банковского счета
- Не требует карты

### 4. Optima Bank
**Тип:** Банковский перевод

```typescript
// Конфигурация
OPTIMA_API_URL=https://api.optimabank.kg
OPTIMA_MERCHANT_ID=your_merchant_id
OPTIMA_SECRET_KEY=your_secret_key
```

**Использование:**
- Традиционный банковский перевод
- Надежность и безопасность

---

## 🗄️ База данных

### Таблица `boost_transactions`

```sql
CREATE TABLE boost_transactions (
  id UUID PRIMARY KEY,
  car_id UUID REFERENCES cars(id),
  user_id UUID REFERENCES users(id),
  boost_type TEXT, -- 'basic' | 'top' | 'premium'
  amount DECIMAL(10,2),
  currency TEXT DEFAULT 'KGS',
  payment_method TEXT, -- 'mbank' | 'bakai' | 'obank' | 'optima'
  payment_id TEXT,
  payment_url TEXT,
  status TEXT, -- 'pending' | 'processing' | 'success' | 'failed'
  activated_at TIMESTAMP,
  expires_at TIMESTAMP,
  duration_hours INTEGER,
  views_before INTEGER,
  views_during INTEGER,
  views_after INTEGER,
  metadata JSONB,
  created_at TIMESTAMP,
  updated_at TIMESTAMP
);
```

### Обновление таблицы `cars`

```sql
ALTER TABLE cars ADD COLUMN boost_type TEXT;
ALTER TABLE cars ADD COLUMN boost_expires_at TIMESTAMP;
ALTER TABLE cars ADD COLUMN boost_activated_at TIMESTAMP;
ALTER TABLE cars ADD COLUMN views_before_boost INTEGER;
```

### Таблица `webhook_logs`

```sql
CREATE TABLE webhook_logs (
  id UUID PRIMARY KEY,
  source TEXT, -- 'mbank' | 'bakai' | 'obank' | 'optima'
  event TEXT,
  payment_id TEXT,
  headers JSONB,
  body JSONB,
  response JSONB,
  ip_address INET,
  success BOOLEAN,
  error TEXT,
  created_at TIMESTAMP
);
```

### SQL Functions

#### `update_car_boost()`
Активирует BOOST на автомобиле и сохраняет текущие просмотры.

#### `cleanup_expired_boosts()`
Деактивирует истекшие BOOST (запускается через cron каждый час).

#### `get_user_boost_stats()`
Возвращает статистику BOOST пользователя.

---

## 🏗️ Архитектура

### Frontend (React Native + Expo)

```
components/Boost/
├── BoostModal.tsx          # Модальное окно выбора тарифа
├── PaymentMethodSelector.tsx  # Выбор способа оплаты
├── BoostBadge.tsx          # Значок на карточке авто
└── BoostStats.tsx          # Статистика эффективности
```

### Services

```
services/payments/
├── index.ts                # Главный модуль
├── mbank.ts                # Интеграция Mbank
├── bakaibank.ts            # Интеграция Bakai Bank
├── obank.ts                # Интеграция O!Bank
├── optimabank.ts           # Интеграция Optima Bank
├── boost.ts                # Логика BOOST
└── webhooks.ts             # Обработчики webhook'ов
```

### Типы

```
types/
├── boost.ts                # Типы для BOOST системы
└── index.ts                # Обновлен с полями BOOST для Car
```

---

## 🔌 Интеграция

### Шаг 1: Применить схему БД

```bash
psql -h your-db-host -U postgres -d your-db < supabase-boost-schema.sql
```

### Шаг 2: Настроить переменные окружения

```bash
# .env или через EAS Secrets

# Mbank
MBANK_API_URL=https://api.mbank.kg
MBANK_API_KEY=your_key
MBANK_MERCHANT_ID=your_id

# Bakai Bank
BAKAI_API_URL=https://balance.kg/api
BAKAI_MERCHANT_ID=your_id
BAKAI_SECRET_KEY=your_key

# O!Bank
OBANK_API_URL=https://api.obank.kg
OBANK_API_KEY=your_key
OBANK_MERCHANT_ID=your_id

# Optima Bank
OPTIMA_API_URL=https://api.optimabank.kg
OPTIMA_MERCHANT_ID=your_id
OPTIMA_SECRET_KEY=your_key
```

### Шаг 3: Интегрировать в экран автомобиля

Уже реализовано в `app/car/[id].tsx`:

```typescript
import BoostModal from '@/components/Boost/BoostModal';
import BoostBadge from '@/components/Boost/BoostBadge';
import { boostService } from '@/services/payments/boost';

// В компоненте:
const [showBoostModal, setShowBoostModal] = useState(false);
const [activeBoost, setActiveBoost] = useState(null);

// Загрузка статуса
const loadBoostStatus = async () => {
  const boost = await boostService.getActiveBoost(carId);
  setActiveBoost(boost);
};

// Отображение
{activeBoost && <BoostBadge type={activeBoost.type} />}

<BoostModal
  visible={showBoostModal}
  onClose={() => setShowBoostModal(false)}
  carId={carId}
  userId={userId}
  carName={carName}
/>
```

---

## 🌐 API Endpoints

### Backend (для webhook'ов)

```typescript
// POST /api/webhooks/mbank
POST https://api.360auto.kg/webhooks/mbank
Content-Type: application/json
{
  "payment_id": "...",
  "status": "success",
  "metadata": {
    "transaction_id": "..."
  }
}

// Аналогично для bakai, obank, optima
```

### Frontend

```typescript
// Создать транзакцию BOOST
const result = await boostService.createBoostTransaction(
  carId,
  userId,
  'top',
  150,
  'mbank'
);

// Получить активный BOOST
const activeBoost = await boostService.getActiveBoost(carId);

// Получить историю
const history = await boostService.getBoostHistory(userId);
```

---

## ⚙️ Настройка

### Cron Job для очистки истекших BOOST

```bash
# На сервере или через Vercel Cron
# Каждый час в :00
0 * * * * curl -X POST https://api.360auto.kg/cron/cleanup-boosts
```

```typescript
// Backend endpoint
app.post('/cron/cleanup-boosts', async (req, res) => {
  const count = await boostService.cleanupExpiredBoosts();
  res.json({ cleaned: count });
});
```

### Push уведомления

```typescript
// Уведомление за 2 часа до истечения BOOST
async function notifyBoostExpiring(userId: string, carId: string) {
  await pushNotifications.send(userId, {
    title: '⏰ BOOST скоро истечет',
    body: 'Ваш BOOST истекает через 2 часа. Продлить?',
    data: { type: 'boost_expiring', carId },
  });
}
```

---

## 🧪 Тестирование

### Development Mode

Все платежные сервисы поддерживают режим разработки:

```typescript
// Если API ключи не настроены или равны 'test_key',
// возвращаются mock данные:

const result = await mbankPayment.createPayment({
  amount: 150,
  description: 'Test payment',
});

// result = {
//   success: true,
//   payment_id: 'mbank_1234567890_abc',
//   payment_url: 'https://pay.mbank.kg/checkout?id=mock_...',
//   status: 'pending'
// }
```

### Тестовые сценарии

1. **Создание BOOST:**
   - Открыть детали своего автомобиля
   - Нажать "🚀 Продвинуть объявление"
   - Выбрать тариф
   - Выбрать способ оплаты
   - Завершить оплату

2. **Webhook тестирование:**
   ```bash
   curl -X POST http://localhost:3000/webhooks/mbank \
     -H "Content-Type: application/json" \
     -d '{
       "payment_id": "test_123",
       "status": "success",
       "metadata": {"transaction_id": "uuid"}
     }'
   ```

3. **Очистка истекших:**
   ```typescript
   const cleaned = await boostService.cleanupExpiredBoosts();
   console.log(`Cleaned ${cleaned} expired boosts`);
   ```

---

## 📊 Прогноз доходов

### Консервативный сценарий (Год 1)

**Предположения:**
- 1000 активных продавцов в месяц
- 10% конверсия в BOOST
- Средний чек: 100 сом

**Расчет:**
```
Месячный доход = 1000 × 10% × 100 = 10,000 сом
Годовой доход = 10,000 × 12 = 120,000 сом
```

### Оптимистичный сценарий (Год 2)

**Предположения:**
- 5000 активных продавцов в месяц
- 20% конверсия в BOOST
- Средний чек: 150 сом

**Расчет:**
```
Месячный доход = 5000 × 20% × 150 = 150,000 сом
Годовой доход = 150,000 × 12 = 1,800,000 сом
```

### Распределение по тарифам (прогноз)

| Тариф | % использования | Средняя цена | Вклад в доход |
|-------|----------------|--------------|---------------|
| ⭐ Выделение | 45% | 50 сом | ~30% |
| 🔥 ТОП | 40% | 150 сом | ~55% |
| 💎 Премиум | 15% | 300 сом | ~15% |

### Ключевые метрики

- **LTV (Lifetime Value):** ~500 сом на продавца
- **CAC (Customer Acquisition Cost):** ~50 сом (органический трафик)
- **ROI:** 10:1 (очень высокий благодаря органике)
- **Payback Period:** < 1 месяц

---

## 🎯 Дорожная карта

### Фаза 1: MVP (Текущая) ✅
- [x] Базовая система BOOST
- [x] Интеграция с 4 банками
- [x] UI/UX компоненты
- [x] Webhook обработка

### Фаза 2: Оптимизация (Q1 2026)
- [ ] A/B тестирование цен
- [ ] Умные рекомендации тарифов
- [ ] Скидки и промокоды
- [ ] Пакетные предложения

### Фаза 3: Расширение (Q2 2026)
- [ ] Автоматическое продление
- [ ] Подписки (Monthly BOOST)
- [ ] Корпоративные тарифы
- [ ] Партнерская программа

### Фаза 4: Аналитика (Q3 2026)
- [ ] Детальная аналитика для продавцов
- [ ] Прогнозирование продаж
- [ ] Конкурентный анализ
- [ ] AI рекомендации по времени активации

---

## 💡 Best Practices

### Для пользователей

1. **Лучшее время для BOOST:**
   - Понедельник-Четверг: 18:00-21:00
   - Выходные: 10:00-16:00

2. **Оптимизация контента:**
   - Качественное видео (важно!)
   - Полное описание
   - Конкурентная цена

3. **Выбор тарифа:**
   - Basic: для быстрой продажи
   - Top: для популярных моделей
   - Premium: для премиум авто

### Для разработчиков

1. **Безопасность:**
   - Всегда валидировать webhook подписи
   - Использовать HTTPS для всех платежей
   - Логировать все транзакции

2. **Производительность:**
   - Кэшировать активные BOOST
   - Использовать индексы БД
   - Асинхронная обработка webhook'ов

3. **Мониторинг:**
   - Отслеживать успешность платежей
   - Алерты при сбоях
   - Метрики конверсии

---

## 📞 Поддержка

Если у вас возникли вопросы или проблемы:

1. Проверьте логи webhook'ов в `webhook_logs` таблице
2. Убедитесь, что API ключи настроены правильно
3. Проверьте статус транзакции в `boost_transactions`
4. Свяжитесь с технической поддержкой банка

---

## 📝 Лицензия

Собственная разработка 360Auto © 2025

---

**Последнее обновление:** Октябрь 2025
**Версия:** 1.0.0
**Статус:** Production Ready ✅
