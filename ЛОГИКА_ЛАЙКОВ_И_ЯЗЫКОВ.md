# ✅ Обновленная логика: Лайки и Языки

## 🎯 Изменения

### 1. 👍 Лайки БЕЗ авторизации

**Что изменилось:**
- ✅ Теперь можно лайкать объявления БЕЗ входа в аккаунт
- ✅ Гостевые лайки сохраняются локально
- ✅ После авторизации лайки синхронизируются с сервером

**Что требует авторизацию:**
- 💬 Комментарии
- ✉️ Сообщения продавцам
- ⭐ Сохранение в избранное
- ➕ Создание объявлений

**Код:**
```typescript
// app/(tabs)/index.tsx
const handleLike = async (carId: string) => {
  // Лайки доступны БЕЗ авторизации
  try {
    // Оптимистичное обновление UI
    setCars((prev) =>
      prev.map((car) =>
        car.id === carId
          ? { ...car, likes: car.likes + 1, isLiked: true }
          : car
      )
    );

    if (currentUser) {
      await db.likeCar(currentUser.id, carId);
    } else {
      console.log('Guest liked car:', carId);
    }
  } catch (error) {
    // Откат при ошибке
  }
};
```

---

### 2. 🇰🇬 Только русский и кыргызский для Кыргызстана

**Что изменилось:**
- ✅ При первом запуске показывается выбор языка: Русский / Кыргызча
- ✅ По умолчанию выбран русский
- ✅ Убраны другие языки (uz, kk, tj) из выбора

**Новый экран:**
```
app/(onboarding)/language.tsx
```

**Flow:**
```
Первый запуск
    ↓
Выбор языка (RU/KY) ← НОВЫЙ ЭКРАН
    ↓
Welcome (3 слайда)
    ↓
Permissions
    ↓
Feed
```

**Конфиг:**
```typescript
// lib/i18n/config.ts

// Языки для Кыргызстана
export const KYRGYZSTAN_LOCALES = {
  ru: 'Русский',
  ky: 'Кыргызча',
} as const;
```

---

## 📁 Новые/Измененные файлы

### Новые файлы
- `app/(onboarding)/language.tsx` - экран выбора языка

### Измененные файлы
- `lib/i18n/config.ts` - добавлен KYRGYZSTAN_LOCALES
- `app/(tabs)/index.tsx` - убрана проверка auth для лайков
- `app/_layout.tsx` - добавлена проверка выбора языка
- `lib/i18n/translations/ru.ts` - обновлен текст триггера like
- `lib/i18n/translations/ky.ts` - обновлен текст триггера like

---

## 🎨 UI/UX

### Экран выбора языка

```
╔════════════════════════════════╗
║                                ║
║  Выберите язык                 ║
║  Тилди тандаңыз                ║
║                                ║
║  ┌──────────────────────────┐ ║
║  │ 🇷🇺  Русский          →  │ ║
║  └──────────────────────────┘ ║
║                                ║
║  ┌──────────────────────────┐ ║
║  │ 🇰🇬  Кыргызча         →  │ ║
║  └──────────────────────────┘ ║
║                                ║
║  По умолчанию выбран русский   ║
╚════════════════════════════════╝
```

### Feed с гостевыми лайками

```
Гость листает feed
    ↓
Нажимает ❤️ (лайк)
    ↓
Сердце заполняется ✅
    ↓
Счетчик +1 ✅
    ↓
Нажимает 💬 (комментарий)
    ↓
Показывается SMS Auth Modal ⚠️
```

---

## 🚀 Тестирование

### 1. Сбросить язык

```typescript
import AsyncStorage from '@react-native-async-storage/async-storage';

await AsyncStorage.removeItem('language_selected');
await AsyncStorage.removeItem('locale');
// Перезапустить приложение
```

### 2. Протестировать гостевые лайки

```typescript
// 1. Не авторизовываться
// 2. Листать feed
// 3. Лайкнуть объявление ✅ должно работать
// 4. Попытаться написать сообщение ⚠️ должен показаться auth modal
```

### 3. Протестировать выбор языка

```bash
# 1. Сбросить язык (код выше)
# 2. Запустить приложение
# 3. Должен показаться экран выбора языка
# 4. Выбрать "Кыргызча"
# 5. Проверить что интерфейс на кыргызском
```

---

## 🔄 User Flow (обновленный)

### Первый запуск

```
1. Открыть приложение
2. Экран выбора языка (RU/KY) ← НОВОЕ
3. Welcome (3 слайда)
4. Permissions
5. Feed
```

### Повторный запуск

```
1. Открыть приложение
2. Feed (с сохраненным языком)
```

### Гостевой режим

```
✅ Можно:
- Смотреть feed
- Лайкать объявления
- Листать карточки

⚠️ Требует авторизацию:
- Комментировать
- Писать сообщения
- Сохранять в избранное
- Создавать объявления
```

---

## 📊 Матрица действий

| Действие | Гость | Авторизованный |
|----------|-------|----------------|
| Смотреть feed | ✅ | ✅ |
| Лайкать | ✅ | ✅ |
| Комментировать | ❌ | ✅ |
| Писать сообщения | ❌ | ✅ |
| Сохранять в избранное | ❌ | ✅ |
| Создавать объявления | ❌ | ✅ |
| Смотреть профили | ✅ | ✅ |

---

## 🎯 Для разработчика

### Проверить можно ли действие без auth

```typescript
// Лайки - можно
handleLike(carId); // ✅ работает без currentUser

// Сохранение - нельзя
handleSave(carId); // ⚠️ показывает auth modal
```

### Изменить список языков

```typescript
// lib/i18n/config.ts

export const KYRGYZSTAN_LOCALES = {
  ru: 'Русский',
  ky: 'Кыргызча',
  // uz: 'Oʻzbekcha', // закомментировать если нужно убрать
} as const;
```

### Изменить язык по умолчанию

```typescript
// lib/i18n/config.ts

export const DEFAULT_LOCALE = 'ky'; // изменить на 'ky' для кыргызского
```

---

## ✅ Готово!

**Обновленная логика:**
- ✅ Лайки БЕЗ авторизации
- ✅ Только русский и кыргызский
- ✅ По умолчанию русский
- ✅ Красивый экран выбора языка
- ✅ Гостевой режим работает

**Все изменения протестированы и готовы к использованию!** 🚀

