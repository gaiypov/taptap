# 📸 План оптимизации Camera UI

**Дата**: 2025-11-25
**Статус**: В разработке

---

## 🎯 Проблемы сейчас:

1. ❌ **Гайды не видны до съемки** - подсказки показываются только во время записи
2. ❌ **UI занимает пол экрана** - кнопки, шрифты, прогресс-бары слишком крупные
3. ❌ **Клавиатура закрывает форму** - нет KeyboardAvoidingView
4. ❌ **Нет визуального гайда** - не показывается что именно снимать (спереди/сзади/салон)

---

## ✅ Решения:

### 1. Добавить визуальный гайд ДО съемки

**Before** (сейчас):
```
[Камера]
↓ Нажми Record
↓ Во время записи показываются hints
```

**After** (будет):
```
[Камера]
↓
📋 Визуальные иконки-подсказки:
   🚗 Спереди → 🚗 Сзади → 🚪 Салон → 🔊 Двигатель
↓ Нажми Record
↓ Минимальный UI во время записи
```

**Фичи**:
- Показывать чек-лист ПЕРЕД записью
- Компактные иконки вместо больших текстов
- Галочки отмечают прогресс

---

### 2. Компактный UI во время съемки

**Before** (сейчас):
- progressContainer: `bottom: 140px` - огромный блок
- recordingHint: большой текстовый блок
- timer: крупный
- Все занимает 50% экрана

**After** (будет):
- progressContainer: `bottom: 110px` - уменьшить на 30px
- recordingHint: только иконка + короткий текст (1-2 слова)
- timer: компактный в углу
- Максимум 20-25% экрана

**Изменения стилей**:
```typescript
// Было:
recordingHint: {
  maxWidth: 280,
  paddingVertical: 14,
  fontSize: 16,
}

// Станет:
recordingHint: {
  maxWidth: 200,
  paddingVertical: 8,
  fontSize: 13,
}

// Было:
progressContainer: {
  bottom: 140,
  padding: 16,
}

// Станет:
progressContainer: {
  bottom: 110,
  padding: 10,
}
```

---

### 3. Исправить клавиатуру в ListingForm

**Проблема**:
```tsx
<KeyboardAvoidingView behavior="padding"> // ❌ Не работает хорошо
  <ScrollView>
    <TextInput /> // Клавиатура закрывает
  </ScrollView>
</KeyboardAvoidingView>
```

**Решение**:
```tsx
<KeyboardAvoidingView
  behavior={Platform.OS === 'ios' ? 'padding' : 'height'}
  keyboardVerticalOffset={Platform.OS === 'ios' ? 100 : 0}
>
  <ScrollView
    keyboardShouldPersistTaps="handled"
    contentContainerStyle={{ paddingBottom: 100 }}
  >
    <TextInput />
  </ScrollView>
</KeyboardAvoidingView>
```

**Ключевые изменения**:
- ✅ `keyboardVerticalOffset` - сдвиг под хедер
- ✅ `keyboardShouldPersistTaps="handled"` - клики работают
- ✅ `contentContainerStyle={{ paddingBottom: 100 }}` - отступ снизу

---

### 4. Адаптивная верстка iOS/Android

**Текущая логика** (хорошая):
```typescript
Platform.select({
  ios: 16,
  android: 14,
  default: 16
})
```

**Добавим**:
- Dimensions для разных размеров экрана
- Проверка на маленькие экраны (< 667px height)
- Пропорциональные размеры вместо фиксированных

```typescript
import { Dimensions } from 'react-native';

const { height: SCREEN_HEIGHT } = Dimensions.get('window');
const IS_SMALL_SCREEN = SCREEN_HEIGHT < 667;

const styles = StyleSheet.create({
  recordingHint: {
    top: IS_SMALL_SCREEN ? 80 : 110,
    paddingVertical: IS_SMALL_SCREEN ? 6 : 8,
    fontSize: IS_SMALL_SCREEN ? 12 : 13,
  },
  progressContainer: {
    bottom: IS_SMALL_SCREEN ? 90 : 110,
    padding: IS_SMALL_SCREEN ? 8 : 10,
  },
});
```

---

## 📋 Checklist для создания улучшений:

### Шаг 1: Визуальный гайд (Pre-recording)
- [ ] Создать компонент `ShootingGuide` с иконками
- [ ] Добавить в CameraCapture до нажатия Record
- [ ] Показывать чек-лист: ✅ Спереди, ✅ Сзади, ✅ Салон, ✅ Двигатель
- [ ] Анимация появления/скрытия

### Шаг 2: Компактный UI (During recording)
- [ ] Уменьшить `recordingHint`: maxWidth 280→200, fontSize 16→13, padding 14→8
- [ ] Уменьшить `progressContainer`: bottom 140→110, padding 16→10
- [ ] Уменьшить `timer`: fontSize 16→14
- [ ] Hints показывать только иконкой + 1-2 слова

### Шаг 3: Keyboard Fix (Form)
- [ ] Добавить `keyboardVerticalOffset` в KeyboardAvoidingView
- [ ] Добавить `contentContainerStyle` в ScrollView
- [ ] Добавить `keyboardShouldPersistTaps="handled"`
- [ ] Тестировать на iOS и Android

### Шаг 4: Адаптивность
- [ ] Добавить `Dimensions.get('window')`
- [ ] Создать `IS_SMALL_SCREEN` флаг
- [ ] Условная логика для маленьких экранов
- [ ] Пропорциональные размеры

---

## 🎨 Mockup нового UI:

```
┌────────────────────────────┐
│ [X]    🚗 Автомобиль    ⏱ │ ← Компактный хедер (40px)
├────────────────────────────┤
│                            │
│                            │
│      📸 КАМЕРА ПРЕВЬЮ      │
│                            │
│                            │
│      🚗→🚗→🚪→🔊           │ ← Иконки-гайд (40px, только ДО записи)
│                            │
│                            │
│                            │
├────────────────────────────┤
│ 💡 Салон (12px, прозр.)   │ ← Компактный hint во время записи (30px)
│ ▓▓▓▓░░░░░░ 0:45 / 2:00   │ ← Компактный прогресс (40px)
│           [●]              │ ← Кнопка Record (80px)
└────────────────────────────┘

ИТОГО: ~190px UI (было ~350px)
```

---

## 🚀 Следующие шаги:

1. **Шаг 1** - Создать компонент ShootingGuide
2. **Шаг 2** - Оптимизировать размеры в CameraCapture
3. **Шаг 3** - Исправить клавиатуру в ListingForm
4. **Шаг 4** - Добавить адаптивность
5. **Тест** - Протестировать на реальном устройстве

---

## 📊 Ожидаемые улучшения:

- **UI занимаемая площадь**: 350px → 190px (сокращение на 46%)
- **Видимость камеры**: 50% → 75% (увеличение на 50%)
- **UX**: Пользователь видит что снимать ДО начала записи
- **Keyboard**: Форма не закрывается клавиатурой
- **Адаптивность**: Работает на всех размерах экранов

**Начать реализацию?** 🚀
