# Backend API Architecture - Security Implementation

## 🔒 Безопасная архитектура AI сервиса

### Проблема безопасности
**API ключи НЕ должны быть в клиентском приложении!**

- Клиентские приложения могут быть декомпилированы
- API ключи могут быть извлечены из кода
- Нет контроля над использованием ключей
- Нарушение условий использования AI провайдеров

### ✅ Решение: Backend API

## Архитектура

```
┌─────────────────┐    HTTPS     ┌─────────────────┐    API Keys    ┌─────────────────┐
│   Mobile App    │ ──────────── │   Backend API   │ ───────────── │   AI Services   │
│   (React Native)│              │   (Node.js)     │               │   (OpenAI, etc) │
└─────────────────┘              └─────────────────┘               └─────────────────┘
```

## Структура проекта

```
backend/
├── api/
│   └── analyze.ts          # AI анализ endpoints
├── services/
│   └── aiService.ts        # AI логика (с API ключами)
├── middleware/
│   ├── auth.ts            # Аутентификация
│   └── validation.ts      # Валидация данных
├── types/
│   └── index.ts           # TypeScript типы
├── server.ts              # Основной сервер
└── package.json           # Зависимости

services/
└── api.ts                 # Клиентский API (без ключей)
```

## Backend API Endpoints

### POST /api/analyze-car
Анализ автомобиля по кадрам видео

**Request:**
```json
{
  "videoFrames": ["data:image/jpeg;base64,...", "..."],
  "metadata": {
    "videoUrl": "file://video.mp4",
    "duration": 30,
    "userId": "user123"
  }
}
```

**Response:**
```json
{
  "success": true,
  "data": {
    "brand": "Toyota",
    "model": "Camry",
    "year": 2020,
    "mileage": 45000,
    "aiAnalysis": {
      "condition": "good",
      "conditionScore": 82,
      "damages": [...],
      "estimatedPrice": {
        "min": 2300000,
        "max": 2600000
      },
      "features": [...]
    }
  }
}
```

### POST /api/quick-identify
Быстрая идентификация по фото

**Request:**
```json
{
  "imageBase64": "data:image/jpeg;base64,..."
}
```

### POST /api/validate-video
Проверка качества видео

**Request:**
```json
{
  "videoMetadata": {
    "duration": 30,
    "width": 1920,
    "height": 1080
  }
}
```

## Безопасность

### 1. Аутентификация
```typescript
// JWT токены для доступа к API
Authorization: Bearer <jwt-token>
```

### 2. Валидация данных
- Проверка формата base64 изображений
- Ограничение размера запросов (50MB)
- Валидация количества кадров (максимум 10)

### 3. Rate Limiting
- 100 запросов в 15 минут с одного IP
- Отдельные лимиты для разных endpoints

### 4. Переменные окружения
```bash
# Backend (.env)
OPENAI_API_KEY=sk-your-key
ANTHROPIC_API_KEY=sk-ant-your-key
GOOGLE_VISION_API_KEY=your-key
JWT_SECRET=your-secret

# Client (.env)
EXPO_PUBLIC_API_URL=http://localhost:3001/api
EXPO_PUBLIC_AI_MODE=production
```

## Клиентский код

### Обновленный API сервис
```typescript
// services/api.ts
export const api = {
  ai: {
    analyzeCar: async (videoUri: string, onProgress?: (step: string, progress: number) => void) => {
      const frames = await extractFramesFromVideo(videoUri);
      
      const response = await apiClient.post('/analyze-car', {
        videoFrames: frames,
        metadata: { videoUrl: videoUri }
      });
      
      return response.data.data;
    },
    
    quickIdentify: async (imageUri: string) => {
      const base64 = await FileSystem.readAsStringAsync(imageUri, {
        encoding: FileSystem.EncodingType.Base64,
      });
      
      const response = await apiClient.post('/quick-identify', {
        imageBase64: `data:image/jpeg;base64,${base64}`,
      });
      
      return response.data.data;
    }
  }
};
```

### Упрощенный AI сервис
```typescript
// services/ai.ts
export async function analyzeCarVideo(videoUri: string, onProgress?: (step: string, progress: number) => void) {
  return await api.ai.analyzeCar(videoUri, onProgress);
}

export async function quickIdentifyCar(imageUri: string) {
  return await api.ai.quickIdentify(imageUri);
}
```

## Развертывание

### 1. Backend сервер
```bash
cd backend
npm install
npm run dev  # для разработки
npm start    # для продакшена
```

### 2. Переменные окружения
```bash
# Backend
NODE_ENV=production
PORT=3001
OPENAI_API_KEY=sk-your-real-key
ANTHROPIC_API_KEY=sk-ant-your-real-key
GOOGLE_VISION_API_KEY=your-real-key
JWT_SECRET=your-super-secret-key

# Client
EXPO_PUBLIC_API_URL=https://your-backend.com/api
EXPO_PUBLIC_AI_MODE=production
```

### 3. Безопасность в продакшене
- Используйте HTTPS для всех запросов
- Ограничьте CORS только вашими доменами
- Настройте firewall для backend сервера
- Используйте reverse proxy (nginx)
- Мониторинг и логирование

## Преимущества новой архитектуры

### 🔒 Безопасность
- API ключи хранятся только на сервере
- Контроль доступа через JWT токены
- Валидация всех входящих данных
- Rate limiting для предотвращения злоупотреблений

### 📈 Масштабируемость
- Backend может обрабатывать множество клиентов
- Кэширование результатов анализа
- Очереди для обработки запросов
- Горизонтальное масштабирование

### 🛠️ Управление
- Централизованное управление AI провайдерами
- Логирование всех запросов
- Мониторинг использования API
- Легкое обновление AI моделей

### 💰 Экономия
- Контроль расходов на AI API
- Оптимизация запросов
- Кэширование результатов
- Batch обработка

## Миграция

### Шаг 1: Развертывание backend
1. Создайте сервер с Node.js
2. Установите зависимости
3. Настройте переменные окружения
4. Запустите сервер

### Шаг 2: Обновление клиента
1. Обновите `services/api.ts`
2. Упростите `services/ai.ts`
3. Обновите переменные окружения
4. Протестируйте интеграцию

### Шаг 3: Тестирование
1. Проверьте аутентификацию
2. Протестируйте все endpoints
3. Проверьте обработку ошибок
4. Настройте мониторинг

## Мониторинг

### Логирование
```typescript
console.log(`🚀 Starting car analysis for user ${userId}`);
console.log(`✅ Analysis completed for user ${userId}`);
console.error('❌ Analysis error:', error);
```

### Метрики
- Количество запросов в минуту
- Время обработки анализа
- Успешность запросов
- Использование AI API

### Алерты
- Превышение лимитов API
- Ошибки аутентификации
- Высокое время отклика
- Недоступность AI сервисов

---

**Важно**: Всегда тестируйте новую архитектуру в staging окружении перед развертыванием в продакшене!
