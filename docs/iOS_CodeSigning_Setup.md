# 🔐 iOS Code Signing Setup Guide

Для сборки на **реальное iOS устройство** нужны сертификаты подписи кода от Apple.

## 🎯 Быстрое решение: Симулятор

Соберите для симулятора (не требует сертификатов):

```bash
npx expo run:ios
```

Это запустит приложение в iOS симуляторе без необходимости настройки сертификатов.

---

## 📱 Для реального устройства: Настройка сертификатов

### Вариант 1: Бесплатный Apple ID (для личного тестирования)

1. **Откройте Xcode:**

   ```bash
   open ios/*.xcworkspace
   ```

2. **Настройте Signing:**
   - Выберите проект в навигаторе
   - Перейдите в **Signing & Capabilities**
   - Включите **Automatically manage signing**
   - Выберите вашу команду разработчика (Team)
   - Если команды нет - добавьте Apple ID: **Xcode → Settings → Accounts**

3. **Соберите через Xcode:**
   - Выберите ваше устройство в списке устройств
   - Нажмите **Run** (▶️)

### Вариант 2: EAS Build (рекомендуется для продакшн)

Используйте облачную сборку через EAS:

```bash
# Настройте EAS (если еще не настроен)
npx eas build:configure

# Соберите development build
eas build --profile development --platform ios
```

После сборки установите IPA на устройство через TestFlight или прямая установка.

---

## ⚙️ Настройка через командную строку

### С Apple Developer Account

```bash
# Войдите в Apple Developer аккаунт через fastlane
fastlane match development

# Или используйте существующие сертификаты
# Убедитесь, что в Xcode настроен ваш Team ID
```

### С бесплатным Apple ID

1. В Xcode: **Settings → Accounts → Add Apple ID**
2. Выберите команду в Signing & Capabilities
3. Xcode автоматически создаст provisioning profile

---

## 🔍 Проверка настроек

```bash
# Проверить доступные сертификаты
security find-identity -v -p codesigning

# Проверить teams
xcodebuild -showBuildSettings | grep DEVELOPMENT_TEAM
```

---

## ✅ Что нужно для успешной сборки

- [ ] Xcode установлен и открыт хотя бы один раз
- [ ] Apple ID добавлен в Xcode (Settings → Accounts)
- [ ] Устройство подключено и доверено (На устройстве: Settings → General → Device Management → Trust)
- [ ] Bundle Identifier уникален (уже настроен: `com.superapp.auto360`)

---

## 🚀 После настройки сертификатов

```bash
npx expo run:ios --device
```

---

## 📝 Примечания

- **Бесплатный Apple ID** позволяет тестировать на реальном устройстве в течение 7 дней
- **Платный Developer Account** ($99/год) нужен для публикации в App Store
- Для разработки достаточно бесплатного аккаунта
- Сертификаты истекают через год и требуют обновления

---

**Совет:** Для быстрого тестирования используйте симулятор. Для продакшн сборок используйте EAS Build.
