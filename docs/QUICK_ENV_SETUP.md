# ⚡ Quick Environment Variables Setup

**Быстрая инструкция по настройке переменных окружения**

---

## 🎯 Что нужно сделать

### 1. Открыть файл `.env`

```bash
cd backend
nano .env  # или используйте ваш любимый редактор
```

### 2. Заполнить переменные Yandex Cloud

Найдите в файле секцию `# Yandex Cloud Video` и заполните:

```bash
# Yandex Cloud Video (NEW - replacing api.video)
YANDEX_OAUTH_TOKEN=ваш_oauth_токен_здесь
YANDEX_FOLDER_ID=ваш_folder_id_здесь
YANDEX_VIDEO_CHANNEL_ID=ваш_channel_id_здесь
YANDEX_CDN_DOMAIN=ваш_cdn_domain.cdn.yandex.net  # Опционально
```

**Как получить:**
- **OAuth Token**: https://oauth.yandex.ru/ → Создать приложение → Скопировать токен
- **Folder ID**: Yandex Cloud Console → Создать папку → Скопировать ID
- **Channel ID**: Yandex Cloud → Video Processing → Создать канал → Скопировать ID

### 3. Заполнить переменные VK Cloud

Найдите в файле секцию `# VK CLOUD STORAGE (Backups)` и заполните:

```bash
# VK Cloud Storage (Backups)
VK_CLOUD_ENDPOINT=https://hb.ru-msk.vkcs.cloud
VK_CLOUD_REGION=ru-msk
VK_CLOUD_ACCESS_KEY=ваш_access_key_здесь
VK_CLOUD_SECRET_KEY=ваш_secret_key_здесь
VK_CLOUD_BUCKET_NAME=360automvp-backups
```

**Как получить:**
- **Endpoint & Region**: VK Cloud → Object Storage → Настройки → Endpoint
- **Access Key & Secret Key**: VK Cloud → Object Storage → Access Keys → Создать ключ
- **Bucket Name**: VK Cloud → Object Storage → Создать bucket → Имя: `360automvp-backups`

### 4. Проверить настройку

```bash
npm run check-cloud-env
```

Должно показать:
```
✅ All required environment variables are set!
```

---

## 📚 Подробные инструкции

Полная инструкция с пошаговыми скриншотами: **`docs/ENV_SETUP_GUIDE.md`**

---

## ⚠️ Важно

1. **Не коммитьте `.env` в git** (уже в `.gitignore`)
2. **Сохраните ключи в безопасном месте**
3. **Используйте разные ключи для dev/prod**

---

## 🆘 Проблемы?

Если скрипт показывает ошибки:

1. Проверьте, что файл `.env` находится в папке `backend/`
2. Убедитесь, что нет лишних пробелов в значениях
3. Проверьте, что все кавычки закрыты
4. Смотрите `docs/ENV_SETUP_GUIDE.md` для деталей

---

**Готово!** После заполнения всех переменных можно тестировать интеграции.

