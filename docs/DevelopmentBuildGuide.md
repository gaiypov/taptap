# 🚀 Development Build Setup Guide

Проект настроен для использования **Development Build** вместо **Expo Go**  
— чтобы нативные модули (expo-notifications, expo-audio, expo-video) работали **без ограничений**.

---

## ✅ Что уже сделано

1. **Установлены зависимости:**
   - `expo-dev-client`
   - `expo-notifications`
   - `expo-device`
   - `expo-constants`
   - `expo-modules-autolinking`

2. **Обновлён `app.json`:**
   - Добавлены плагины `expo-dev-client` и `expo-notifications`

3. **Добавлены утилиты:**
   - `utils/testNotification.ts` — утилита тестирования уведомлений
   - `app/test-notifications.tsx` — тестовый экран уведомлений

---

## 📋 Следующие шаги

### 🧱 1. Prebuild (создание нативных проектов)

```bash
npx expo prebuild --clean
```

Эта команда:

- Создаст папки `ios/` и `android/` с нативными проектами
- Настроит все плагины и зависимости
- Подготовит проект для локальной сборки

**Примечание:** После prebuild папки `ios/` и `android/` будут добавлены в `.gitignore` (не коммитятся в репозиторий).

---

### 🔨 2. Сборка Development Build

#### **Для iOS:**

**Симулятор:**

```bash
npx expo run:ios
```

**Реальное устройство:**

```bash
npx expo run:ios --device
```

**Требования:**

- macOS с установленным Xcode
- Xcode Command Line Tools: `xcode-select --install`
- CocoaPods (установится автоматически): `sudo gem install cocoapods`

#### **Для Android:**

**Эмулятор:**

```bash
npx expo run:android
```

**Реальное устройство:**

```bash
npx expo run:android --device
```

**Требования:**

- Android Studio установлен
- Android SDK настроен
- Переменная окружения `ANDROID_HOME` установлена
- Устройство подключено с включенным USB Debugging

---

### ☁️ 3. Использование EAS Build (облачная сборка)

Если хотите собрать Development Build в облаке (не требуется локальная установка Xcode/Android Studio):

#### Настройка EAS

```bash
# Установите EAS CLI (если еще не установлен)
npm install -g eas-cli

# Войдите в аккаунт Expo
eas login

# Настройте EAS для проекта
eas build:configure
```

#### Сборка через EAS

```bash
# iOS
eas build --profile development --platform ios

# Android
eas build --profile development --platform android

# Обе платформы
eas build --profile development --platform all
```

После сборки скачайте APK/IPA и установите на устройство.

**Примечание:** Убедитесь, что в `app.json` указан правильный `projectId` в секции `extra.eas`.

---

### ▶️ 4. Запуск приложения

После установки Development Build на устройство:

```bash
npx expo start --dev-client
```

Или просто:

```bash
npx expo start
```

(автоматически определит использование dev client)

**Важно:** Development Build подключается к Metro bundler так же, как Expo Go. Hot Reload и Fast Refresh работают!

---

### 🧪 5. Тестирование уведомлений

1. Откройте приложение в Development Build
2. Перейдите на экран `/test-notifications`
3. Нажмите кнопку **"Отправить тестовое уведомление"**
4. Должно появиться уведомление с текстом **"🚀 Development Build работает!"**

Если уведомление работает — всё настроено правильно! ✅

---

## 💻 Использование в коде

### Базовое использование уведомлений

```typescript
import { sendTestNotification } from '@/utils/testNotification';

// Отправить тестовое уведомление
await sendTestNotification();
```

### Полная настройка уведомлений

```typescript
import * as Notifications from 'expo-notifications';
import { useEffect } from 'react';

// Настройка обработчика уведомлений
Notifications.setNotificationHandler({
  handleNotification: async () => ({
    shouldShowAlert: true,
    shouldPlaySound: true,
    shouldSetBadge: true,
  }),
});

// Запрос разрешений
const { status } = await Notifications.requestPermissionsAsync();

// Отправка уведомления
await Notifications.scheduleNotificationAsync({
  content: {
    title: 'Заголовок',
    body: 'Текст уведомления',
    data: { customData: 'value' },
  },
  trigger: null, // Отправить немедленно
});
```

---

## ⚠️ Важные замечания

### Отличия от Expo Go

| Expo Go | Development Build |
|---------|-------------------|
| Ограниченный набор модулей | Все нативные модули работают |
| Нельзя кастомизировать нативный код | Полный доступ к нативному коду |
| Быстрый старт | Требует сборки перед первым запуском |
| Обновления через магазины | Установка через сборку |

### Что изменилось

1. ✅ **Expo Go больше не нужен** — используйте Development Build
2. ✅ **Первая сборка** может занять время (10-30 минут)
3. ✅ **Hot Reload** работает так же, как в Expo Go
4. ✅ **Все нативные модули** работают без ограничений

---

## 🔍 Проверка успешной настройки

### Признаки правильной настройки

- ✅ Нет предупреждений о том, что `expo-notifications` не поддерживается в Expo Go
- ✅ Уведомления работают на тестовом экране (`/test-notifications`)
- ✅ Все нативные модули (`expo-audio`, `expo-video`) работают
- ✅ В консоли Metro bundler нет ошибок загрузки нативных модулей

### Проверка в коде

```typescript
import * as Device from 'expo-device';

// Проверить, что это Development Build
if (__DEV__ && Device.isDevice) {
  console.log('✅ Running in Development Build');
}
```

---

## 🐛 Troubleshooting

### Проблема: "Expo Go is not installed"

**Решение:** Используйте Development Build, а не Expo Go. Установите Development Build на устройство.

---

### Проблема: Уведомления не работают

**Возможные причины и решения:**

1. **Нет разрешений:**

   ```typescript
   // Проверьте разрешения
   const { status } = await Notifications.getPermissionsAsync();
   if (status !== 'granted') {
     await Notifications.requestPermissionsAsync();
   }
   ```

2. **Используется Expo Go вместо Development Build:**
   - Убедитесь, что установлен Development Build
   - Перезапустите Metro bundler с флагом `--dev-client`

3. **iOS Simulator не поддерживает push-уведомления:**
   - Используйте реальное устройство для тестирования уведомлений на iOS

---

### Проблема: Ошибка при prebuild

**Решение:**

```bash
# Очистить кеш
rm -rf node_modules
npm install

# Пересобрать
npx expo prebuild --clean
```

---

### Проблема: Ошибка сборки iOS

**Решение:**

```bash
# Установить pods
cd ios
pod install
cd ..

# Пересобрать
npx expo run:ios
```

---

### Проблема: Ошибка сборки Android

**Решение:**

```bash
# Очистить Gradle кеш
cd android
./gradlew clean
cd ..

# Пересобрать
npx expo run:android
```

---

### Проблема: Metro bundler не подключается

**Решение:**

1. Убедитесь, что устройство и компьютер в одной сети
2. Проверьте, что порт 8081 не заблокирован файрволом
3. Попробуйте использовать туннель:

   ```bash
   npx expo start --tunnel
   ```

---

## 📚 Полезные ссылки

- [Expo Development Build Documentation](https://docs.expo.dev/develop/development-builds/introduction/)
- [expo-notifications API Reference](https://docs.expo.dev/versions/latest/sdk/notifications/)
- [expo-audio API Reference](https://docs.expo.dev/versions/latest/sdk/audio/)
- [expo-video API Reference](https://docs.expo.dev/versions/latest/sdk/video/)
- [EAS Build Documentation](https://docs.expo.dev/build/introduction/)
- [Expo Prebuild Documentation](https://docs.expo.dev/workflow/prebuild/)

---

## 🎯 Checklist

Перед началом работы убедитесь, что:

- [ ] Установлены все зависимости (`npm install`)
- [ ] Выполнен prebuild (`npx expo prebuild --clean`)
- [ ] Development Build собран и установлен на устройство
- [ ] Metro bundler запущен (`npx expo start --dev-client`)
- [ ] Уведомления протестированы (`/test-notifications`)
- [ ] Все нативные модули работают корректно

---

## 📝 Примечания

- **Версия Expo SDK:** 54.0.0
- **Минимальная версия iOS:** 13.0
- **Минимальная версия Android:** API 21 (Android 5.0)

---

**Готово!** Теперь ваш проект готов к работе с Development Build и всеми нативными модулями. 🚀
