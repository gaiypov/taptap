# 🔍 Настройка ESLint в Cursor/VS Code

## ✅ Что уже настроено

1. **ESLint конфигурация** (`eslint.config.js`) с строгими правилами
2. **Настройки Cursor/VS Code** (`.vscode/settings.json`) для автоматической проверки
3. **CI Pipeline** (`.github/workflows/ci.yml`) с блокировкой merge при ошибках

---

## 🚀 Быстрая настройка

### 1. Установите расширение ESLint в Cursor

1. Откройте Cursor
2. Нажмите `Cmd+Shift+X` (Mac) или `Ctrl+Shift+X` (Windows/Linux)
3. Найдите и установите: **ESLint** (by Microsoft)
4. Перезапустите Cursor

**Или используйте рекомендованные расширения:**
- Cursor автоматически предложит установить расширения из `.vscode/extensions.json`

### 2. Проверьте, что ESLint работает

1. Откройте любой `.ts` или `.tsx` файл
2. Создайте неиспользуемую переменную:
   ```typescript
   const unusedVar = 123; // Должна появиться красная волнистая линия
   ```
3. Если видите ошибку — ESLint работает! ✅

---

## 📋 Что проверяет ESLint

### Автоматически обнаруживает:

✅ **Мертвый код:**
- Неиспользуемые переменные
- Неиспользуемые функции
- Недостижимый код
- Неиспользуемые импорты

✅ **Проблемы с React Hooks:**
- Нарушение правил хуков
- Отсутствующие зависимости в useEffect/useCallback

✅ **Проблемы с TypeScript:**
- Использование `any`
- Неиспользуемые типы
- Неправильные типы

✅ **Проблемы с импортами:**
- Дублирующиеся импорты
- Неправильный порядок импортов

✅ **Общие проблемы:**
- `console.log` (предупреждение)
- `debugger` (ошибка)
- Использование `var` вместо `const/let`

---

## ⚙️ Настройки Cursor/VS Code

Файл `.vscode/settings.json` уже настроен для:

- ✅ **Автоматическое исправление** при сохранении
- ✅ **Проверка в реальном времени** (при наборе)
- ✅ **Подсветка проблем** в редакторе
- ✅ **Автоформатирование** кода

### Что происходит при сохранении файла:

1. ESLint автоматически исправляет все, что может
2. Импорты автоматически упорядочиваются
3. Код автоматически форматируется

---

## 🔧 Полезные команды

### В терминале:

```bash
# Проверка всех файлов
npm run lint

# Строгая проверка (0 warnings)
npm run lint:strict

# Автоисправление проблем
npm run lint:fix

# Проверка типов
npm run typecheck

# Проверка сборки
npm run build:check
```

### В Cursor (Command Palette: `Cmd+Shift+P`):

- **ESLint: Fix all auto-fixable Problems** — исправить все проблемы
- **ESLint: Show Output Channel** — показать вывод ESLint
- **Format Document** — отформатировать документ

---

## 🚨 CI Pipeline и блокировка Merge

### Что проверяется в CI:

1. ✅ **ESLint** — все файлы (`npm run lint:strict`)
2. ✅ **TypeScript** — проверка типов (`npm run typecheck`)
3. ✅ **Frontend Build** — проверка сборки (`npm run build:check`)
4. ✅ **Backend Build** — проверка сборки backend

### Настройка Branch Protection:

1. GitHub → **Settings** → **Branches**
2. **Add Branch Protection Rule**
3. Выберите ветки: `main`, `dev`
4. Включите:
   - ☑ **Require status checks to pass before merging**
   - Выберите: `lint-and-typecheck`
   - ☑ **Require branches to be up to date before merging**
   - ☑ **Do not allow bypassing the above settings**

**Результат:** PR не может быть смержен, пока все проверки не пройдут ✅

---

## 🐛 Решение проблем

### Проблема: ESLint не работает в Cursor

**Решение:**
1. Убедитесь, что расширение ESLint установлено
2. Перезапустите Cursor
3. Проверьте, что файл открыт (не в preview режиме)
4. Проверьте Output: `View` → `Output` → выберите "ESLint"

### Проблема: ESLint показывает ошибки, но не исправляет

**Решение:**
1. Убедитесь, что в настройках включено:
   ```json
   "editor.codeActionsOnSave": {
     "source.fixAll.eslint": "explicit"
   }
   ```
2. Сохраните файл (`Cmd+S` / `Ctrl+S`)

### Проблема: Слишком много предупреждений

**Решение:**
```bash
# Исправьте все автоматически исправимые проблемы
npm run lint:fix

# Проверьте, что осталось
npm run lint
```

---

## 📝 Примеры правил ESLint

### Обнаружение мертвого кода:

```typescript
// ❌ Ошибка: неиспользуемая переменная
const unused = 123;

// ✅ Правильно: используйте переменную или удалите
const used = 123;
console.log(used);

// ✅ Или используйте префикс _ для игнорирования
const _unused = 123; // ESLint проигнорирует
```

### React Hooks:

```typescript
// ❌ Ошибка: отсутствует зависимость
useEffect(() => {
  fetchData(userId);
}, []); // userId отсутствует в зависимостях

// ✅ Правильно
useEffect(() => {
  fetchData(userId);
}, [userId]);
```

### Импорты:

```typescript
// ❌ Ошибка: дублирующийся импорт
import { useState } from 'react';
import { useEffect } from 'react';

// ✅ Правильно
import { useState, useEffect } from 'react';
```

---

## 🎯 Рекомендации

1. **Всегда исправляйте ошибки ESLint** перед коммитом
2. **Используйте `npm run lint:fix`** для автоматического исправления
3. **Проверяйте CI статус** перед созданием PR
4. **Не игнорируйте предупреждения** — они могут стать ошибками

---

## 📚 Дополнительные ресурсы

- [ESLint документация](https://eslint.org/docs/latest/)
- [TypeScript ESLint правила](https://typescript-eslint.io/rules/)
- [React Hooks ESLint правила](https://www.npmjs.com/package/eslint-plugin-react-hooks)

