# 🔧 Исправление ошибки 502 — Бэкенд не отвечает

## Проблема

Ошибка `{"status":502,"statusText":"","error":{}}` означает, что фронтенд не может подключиться к бэкенду.

## Причины

1. **Бэкенд не запущен** — самая частая причина
2. **Неправильный IP адрес** в `app.json` — на телефоне `localhost` не работает!
3. **Порт 3001 занят** другим процессом
4. **Файрвол блокирует** подключение

## ✅ Решение за 3 минуты

### Шаг 1: Запустите бэкенд

```bash
cd backend
npm run dev
```

Должно появиться:
```
🚀 360⁰ Marketplace API Server Started
🌐 Listening on port: 3001
```

### Шаг 2: Проверьте IP адрес в app.json

**ВАЖНО:** На телефоне `localhost` — это телефон, а не ваш компьютер!

1. Узнайте свой локальный IP:
   - **Mac/Linux**: `ifconfig | grep "inet " | grep -v 127.0.0.1`
   - **Windows**: `ipconfig` → найдите IPv4 адрес
   - **Пример**: `192.168.1.16`

2. Откройте `app.json` и проверьте:
```json
{
  "expo": {
    "extra": {
      "apiUrl": "http://192.168.1.16:3001/api"  // ← ВАШ IP!
    }
  }
}
```

3. Перезапустите Expo после изменения:
```bash
# Остановите текущий процесс (Ctrl+C)
# Запустите снова
npx expo start --clear
```

### Шаг 3: Проверьте доступность бэкенда

Используйте скрипт проверки:
```bash
./scripts/check-backend.sh
```

Или вручную:
```bash
curl http://192.168.1.16:3001/health
```

Должен вернуться JSON с `"status": "healthy"`.

### Шаг 4: Проверьте порт

Если порт 3001 занят:
```bash
# Mac/Linux
lsof -ti:3001 | xargs kill -9

# Windows
netstat -ano | findstr :3001
taskkill /PID <PID> /F
```

## 🧪 Тестирование SMS без фронтенда

Чтобы убедиться, что бэкенд работает:

```bash
curl -X POST http://192.168.1.16:3001/api/sms/send \
  -H "Content-Type: application/json" \
  -d '{"phone":"+996555123456","message":"Тест от Grok"}'
```

Или для запроса кода верификации:
```bash
curl -X POST http://192.168.1.16:3001/api/auth/request-code \
  -H "Content-Type: application/json" \
  -d '{"phone":"996555123456"}'
```

## 📱 Настройка для разных устройств

### Для физического телефона:
- Используйте локальный IP вашего компьютера
- Убедитесь, что телефон и компьютер в одной Wi-Fi сети

### Для эмулятора iOS (Simulator):
- Можно использовать `localhost:3001`
- Или локальный IP компьютера

### Для эмулятора Android:
- Используйте `10.0.2.2:3001` (специальный адрес для Android эмулятора)
- Или локальный IP компьютера

## 🔍 Диагностика

### Проверка в коде

Все сервисы используют API URL из `app.json`:
- `services/sms.ts` — `/api/sms/send`
- `services/auth.ts` — `/api/auth/request-code`
- `services/api.ts` — базовый API клиент

### Логи ошибок

При ошибке 502 вы увидите:
```
❌ Backend unavailable: { status: 502, apiUrl: "http://..." }
```

Или:
```
Не удалось подключиться к бэкенду на http://...
Убедитесь, что сервер запущен: cd backend && npm run dev
```

## 🚀 Production

В production используйте реальный домен:
```json
{
  "expo": {
    "extra": {
      "apiUrl": "https://api.360auto.kg/api"
    }
  }
}
```

## 📝 Чеклист

- [ ] Бэкенд запущен (`npm run dev` в папке `backend`)
- [ ] Порт 3001 свободен
- [ ] Правильный IP в `app.json` (не `localhost` для физических устройств)
- [ ] Телефон и компьютер в одной Wi-Fi сети
- [ ] `curl http://YOUR_IP:3001/health` возвращает `{"status":"healthy"}`
- [ ] Перезапущен Expo после изменения `app.json`

## 🆘 Если ничего не помогает

1. Проверьте файрвол:
   ```bash
   # Mac
   sudo /usr/libexec/ApplicationFirewall/socketfilterfw --getglobalstate
   ```

2. Проверьте, что бэкенд слушает на всех интерфейсах:
   - В `backend/src/index.ts` должно быть `server.listen(PORT)` (без указания хоста)
   - Или `server.listen(PORT, '0.0.0.0')` для явного указания

3. Проверьте логи бэкенда на наличие ошибок

4. Попробуйте другой порт (например, 3002) и обновите `app.json`

