# Применение миграции Consent Audit & RLS

## Пошаговая инструкция

### Шаг 1: Откройте Supabase Dashboard

1. Перейдите на [https://app.supabase.com](https://app.supabase.com)
2. Войдите в свой аккаунт
3. Выберите проект **360AutoMVP** (или ваш проект)

### Шаг 2: Откройте SQL Editor

1. В левом меню найдите **SQL Editor**
2. Нажмите на него
3. Нажмите **New query** (или используйте существующий редактор)

### Шаг 3: Скопируйте миграцию

1. Откройте файл `supabase/migrations/20250120_consent_audit_and_rls.sql`
2. Скопируйте **весь** содержимое файла (Ctrl+A, Ctrl+C / Cmd+A, Cmd+C)
3. Вставьте в SQL Editor в Supabase

### Шаг 4: Проверьте миграцию

Убедитесь, что в редакторе видно:
- Создание таблицы `consent_audit_log`
- Обновление таблицы `user_consents`
- Включение RLS
- Создание политик безопасности

### Шаг 5: Выполните миграцию

1. Нажмите кнопку **Run** (или F5 / Ctrl+Enter)
2. Дождитесь завершения выполнения
3. Проверьте результат в нижней панели

### Шаг 6: Проверьте результат

Выполните следующие запросы для проверки:

```sql
-- Проверка, что таблица audit log создана
SELECT * FROM consent_audit_log LIMIT 1;

-- Проверка структуры user_consents
SELECT column_name, data_type 
FROM information_schema.columns 
WHERE table_name = 'user_consents' 
ORDER BY ordinal_position;

-- Проверка RLS
SELECT tablename, rowsecurity 
FROM pg_tables 
WHERE schemaname = 'public' 
AND tablename IN ('user_consents', 'consent_audit_log');

-- Проверка политик
SELECT schemaname, tablename, policyname 
FROM pg_policies 
WHERE tablename IN ('user_consents', 'consent_audit_log');
```

### Ожидаемый результат

✅ Таблица `consent_audit_log` создана  
✅ В таблице `user_consents` есть колонка `processing_accepted` (или переименована из `consent_accepted`)  
✅ RLS включен на обеих таблицах  
✅ Политики безопасности созданы

### Возможные ошибки и решения

#### Ошибка: "relation already exists"
- **Причина**: Таблица уже существует
- **Решение**: Миграция использует `IF NOT EXISTS`, поэтому это не критично. Продолжайте.

#### Ошибка: "column does not exist"
- **Причина**: Таблица `user_consents` может иметь другую структуру
- **Решение**: Миграция автоматически добавит недостающие колонки. Проверьте результат.

#### Ошибка: "permission denied"
- **Причина**: Недостаточно прав
- **Решение**: Убедитесь, что используете правильный проект и у вас есть права администратора.

### После применения миграции

1. **Добавьте переменные окружения** в `.env`:
   ```env
   CONSENT_TERMS_VERSION=2025.11.01
   CONSENT_PRIVACY_VERSION=2025.11.01
   CONSENT_PROCESSING_VERSION=2025.11.01
   ```

2. **Перезапустите backend** (если он запущен)

3. **Протестируйте API**:
   ```bash
   # Проверка статуса согласий
   curl -X GET http://localhost:3000/api/consents/status \
     -H "Authorization: Bearer YOUR_TOKEN"
   ```

### Проверка работы аудита

После применения миграции, когда пользователь примет согласие, проверьте аудит-лог:

```sql
SELECT * FROM consent_audit_log 
ORDER BY created_at DESC 
LIMIT 10;
```

Должны появиться записи с действиями `accept`, `revoke`, `reconsent_required`.

## Готово! 🎉

Миграция применена. Система согласий теперь соответствует требованиям GDPR и законов КР.

