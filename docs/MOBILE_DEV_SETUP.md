# 📱 Запуск приложения на мобильном устройстве

## 🚀 Быстрый старт

### Вариант 1: Expo Go (простой способ, но ограниченный)

**Для iOS:**
1. Установите **Expo Go** из App Store
2. Запустите dev server:
   ```bash
   npm start
   ```
3. Отсканируйте QR-код в терминале через камеру iPhone
4. Или нажмите `i` в терминале для открытия в iOS симуляторе

**Для Android:**
1. Установите **Expo Go** из Google Play
2. Запустите dev server:
   ```bash
   npm start
   ```
3. Отсканируйте QR-код в терминале через приложение Expo Go
4. Или нажмите `a` в терминале для открытия в Android эмуляторе

**⚠️ Ограничения Expo Go:**
- Не поддерживает все нативные модули
- Может не работать с `expo-dev-client`
- Ограниченная производительность

---

### Вариант 2: Development Build (рекомендуется) ✅

Ваш проект использует `expo-dev-client`, поэтому рекомендуется использовать Development Build.

#### Шаг 1: Установка зависимостей

```bash
npm install
```

#### Шаг 2: Запуск Development Build

**Для iOS:**
```bash
# Сборка и запуск на iOS симуляторе
npm run ios

# Или запуск dev server с dev client
npm run start:dev-client
# Затем нажмите 'i' для iOS
```

**Для Android:**
```bash
# Сборка и запуск на Android эмуляторе
npm run android

# Или запуск dev server с dev client
npm run start:dev-client
# Затем нажмите 'a' для Android
```

#### Шаг 3: Подключение физического устройства

**iOS (через USB):**
1. Подключите iPhone через USB
2. Убедитесь, что устройство доверяет компьютеру
3. Запустите:
   ```bash
   npm run ios
   ```
4. Выберите ваше устройство из списка

**Android (через USB или Wi-Fi):**
1. Включите **Отладка по USB** в настройках разработчика
2. Подключите устройство через USB
3. Запустите:
   ```bash
   npm run android
   ```

**Android (через Wi-Fi):**
1. Убедитесь, что устройство и компьютер в одной сети Wi-Fi
2. Запустите:
   ```bash
   npm start
   ```
3. Нажмите `a` и выберите устройство
4. Или отсканируйте QR-код в Expo Go (если установлен)

---

## 🔧 Полезные команды

### Основные команды запуска:

```bash
# Стандартный запуск (Expo Go)
npm start

# Development Build
npm run start:dev-client

# Запуск на iOS
npm run ios

# Запуск на Android
npm run android

# Запуск на Web
npm run web
```

### Интерактивные команды в Expo CLI:

После запуска `npm start` доступны команды:
- `i` - открыть в iOS симуляторе
- `a` - открыть в Android эмуляторе/устройстве
- `w` - открыть в веб-браузере
- `r` - перезагрузить приложение
- `m` - открыть меню разработчика
- `j` - открыть debugger
- `c` - очистить кэш

---

## 🌐 Подключение через сеть

### Настройка для работы по Wi-Fi:

1. **Убедитесь, что устройство и компьютер в одной сети Wi-Fi**

2. **Узнайте IP адрес компьютера:**
   ```bash
   # macOS/Linux
   ifconfig | grep "inet " | grep -v 127.0.0.1
   
   # Windows
   ipconfig
   ```

3. **Запустите Expo с явным указанием IP:**
   ```bash
   npx expo start --tunnel
   # или
   npx expo start --lan
   ```

4. **Отсканируйте QR-код** или введите URL вручную в Expo Go

---

## 🐛 Решение проблем

### Проблема: "Unable to connect to Metro"

**Решение:**
```bash
# Очистите кэш и перезапустите
npx expo start -c
```

### Проблема: "Network request failed"

**Решение:**
1. Проверьте, что устройство и компьютер в одной сети
2. Отключите VPN на устройстве или компьютере
3. Используйте `--tunnel` режим:
   ```bash
   npx expo start --tunnel
   ```

### Проблема: "Device not found"

**Решение для Android:**
```bash
# Проверьте подключенные устройства
adb devices

# Если устройство не видно, перезапустите adb
adb kill-server
adb start-server
```

**Решение для iOS:**
- Убедитесь, что Xcode установлен
- Проверьте, что устройство доверяет компьютеру
- Откройте Xcode → Window → Devices and Simulators

### Проблема: "Metro bundler error"

**Решение:**
```bash
# Очистите все кэши
rm -rf node_modules
npm install
npx expo start -c
```

---

## 📱 Альтернативные способы

### Использование EAS Build (для тестирования на реальных устройствах):

1. Установите EAS CLI:
   ```bash
   npm install -g eas-cli
   ```

2. Войдите в аккаунт:
   ```bash
   eas login
   ```

3. Создайте development build:
   ```bash
   eas build --profile development --platform ios
   # или
   eas build --profile development --platform android
   ```

4. Установите build на устройство и подключитесь к dev server

---

## 🎯 Рекомендации для вашего проекта

Ваш проект использует:
- ✅ `expo-dev-client` - используйте Development Build
- ✅ `expo-router` - работает в обоих режимах
- ✅ Нативные модули (camera, video, etc.) - требуется Development Build

**Рекомендуемый workflow:**
1. Для разработки: используйте `npm run ios` или `npm run android`
2. Для быстрого тестирования UI: используйте `npm start` + Expo Go (если нет нативных модулей)
3. Для production-тестирования: используйте EAS Build

---

## 📞 Быстрая справка

```bash
# Запуск на физическом устройстве (iOS)
npm run ios

# Запуск на физическом устройстве (Android)  
npm run android

# Запуск dev server (для Expo Go или Development Build)
npm start

# Очистка кэша и перезапуск
npx expo start -c
```

