# 🟡 Yandex Cloud Setup для Центральной Азии (Кыргызстан)

**Полная инструкция по настройке Yandex Cloud для 360AutoMVP**

---

## 🎯 Что нужно от Yandex Cloud

### ✅ Обязательные сервисы:

1. **Yandex Cloud Account** (аккаунт)
2. **Video Processing** (обработка видео)
3. **Object Storage** (хранение файлов) - опционально
4. **CDN** (доставка контента) - опционально, но рекомендуется

---

## 📋 Пошаговая настройка

### ШАГ 1: Создание аккаунта Yandex Cloud

1. **Перейдите на**: https://cloud.yandex.ru/
2. **Войдите** через Yandex аккаунт (или создайте новый)
3. **Создайте Cloud** (если еще нет)
4. **Настройте биллинг**:
   - Добавьте платежный метод (карта)
   - Установите лимиты расходов (рекомендуется: $1000/месяц)
   - Включите уведомления о расходах

**Регион для проекта:**
- **Рекомендуется**: `ru-central1` (Москва) - лучшая производительность для CIS
- **Альтернатива**: `ru-east1` (Владивосток) - ближе к Кыргызстану

---

### ШАГ 2: Получение OAuth Token

**Для автоматического обновления IAM токенов:**

1. **Перейдите на**: https://oauth.yandex.ru/
2. **Нажмите**: "Зарегистрировать новое приложение"
3. **Заполните форму**:
   ```
   Название: 360AutoMVP Backend
   Платформа: Веб-сервисы
   Redirect URI: https://oauth.yandex.ru/verification_code
   ```
4. **Сохраните** и скопируйте **OAuth Token**

**Добавить в `.env`:**
```bash
YANDEX_OAUTH_TOKEN=y0_AgA...ваш_токен_здесь
```

---

### ШАГ 3: Создание Folder (Папки)

**Folder - это контейнер для ресурсов в Yandex Cloud:**

1. **Откройте**: https://console.cloud.yandex.ru/
2. **Выберите** ваш Cloud
3. **Нажмите**: "Создать папку"
4. **Заполните**:
   ```
   Имя: 360automvp-production
   Описание: Production environment for 360AutoMVP
   ```
5. **Скопируйте** Folder ID (формат: `b1g1234567890abcdef`)

**Добавить в `.env`:**
```bash
YANDEX_FOLDER_ID=b1g1234567890abcdef
```

---

### ШАГ 4: Создание Video Channel (Канала)

**Video Channel - это канал для загрузки и обработки видео:**

1. **В Yandex Cloud Console** перейдите в **Video Processing**
2. **Если сервис не включен**:
   - Перейдите в "Каталог сервисов"
   - Найдите "Video Processing"
   - Нажмите "Включить"
3. **Создайте канал**:
   - Нажмите "Создать канал"
   - Заполните:
     ```
     Имя: 360AutoMVP Videos
     Папка: выберите папку из ШАГ 3
     Регион: ru-central1 (Москва)
     ```
4. **Скопируйте** Channel ID (формат: `ch1234567890abcdef`)

**Добавить в `.env`:**
```bash
YANDEX_VIDEO_CHANNEL_ID=ch1234567890abcdef
```

---

### ШАГ 5: (Опционально) Настройка CDN

**CDN ускорит доставку видео в Кыргызстане и Центральной Азии:**

1. **В Yandex Cloud Console** перейдите в **CDN**
2. **Создайте CDN ресурс**:
   - Нажмите "Создать ресурс"
   - Заполните:
     ```
     Имя: 360automvp-cdn
     Источник: Video Processing канал (из ШАГ 4)
     Домены: выберите или создайте домен
     ```
3. **Настройте кэширование**:
   - HLS сегменты: 1 год
   - Манифесты: 5 минут
   - Thumbnails: 1 год
4. **Скопируйте** CDN домен (формат: `xxxxx.cdn.yandex.net`)

**Добавить в `.env` (опционально):**
```bash
YANDEX_CDN_DOMAIN=xxxxx.cdn.yandex.net
```

---

## 🔑 Что нужно скопировать в .env

После настройки у вас должно быть:

```bash
# ============================================
# YANDEX CLOUD VIDEO
# ============================================
YANDEX_OAUTH_TOKEN=y0_AgA...ваш_oauth_токен
YANDEX_FOLDER_ID=b1g1234567890abcdef
YANDEX_VIDEO_CHANNEL_ID=ch1234567890abcdef
YANDEX_CDN_DOMAIN=xxxxx.cdn.yandex.net  # Опционально
```

---

## 💰 Ориентировочные расходы

**Для 300K пользователей, 100K видео:**

| Сервис | Использование | Стоимость/месяц |
|--------|---------------|-----------------|
| Video Processing (Storage) | 5TB | ~$100 |
| Video Processing (Bandwidth) | 500TB | ~$5,000 |
| Video Processing (Transcoding) | 200K минут | ~$2,000 |
| CDN (опционально) | 500TB | ~$1,000 |
| **ИТОГО** | | **~$8,100/месяц** |

**Экономия по сравнению с api.video: ~$52,400/месяц** 💰

---

## 🌍 Региональные настройки для Центральной Азии

### Рекомендуемые регионы:

1. **ru-central1 (Москва)** - ✅ **РЕКОМЕНДУЕТСЯ**
   - Лучшая производительность для CIS
   - Низкая латентность до Кыргызстана (~50-80ms)
   - Хорошая инфраструктура

2. **ru-east1 (Владивосток)**
   - Ближе географически
   - Может быть дороже

### CDN для Центральной Азии:

Yandex CDN автоматически оптимизирует доставку для:
- 🇰🇬 Кыргызстан
- 🇰🇿 Казахстан
- 🇺🇿 Узбекистан
- 🇹🇯 Таджикистан
- 🇹🇲 Туркменистан

**Латентность из Кыргызстана:**
- Без CDN: ~80-120ms
- С CDN: ~30-50ms ✅

---

## ✅ Чеклист настройки

- [ ] Yandex Cloud аккаунт создан
- [ ] Биллинг настроен (карта добавлена)
- [ ] OAuth токен получен
- [ ] Folder создан (ID скопирован)
- [ ] Video Processing сервис включен
- [ ] Video Channel создан (ID скопирован)
- [ ] CDN настроен (опционально, домен скопирован)
- [ ] Все переменные добавлены в `.env`
- [ ] Проверка пройдена: `npm run check-cloud-env`

---

## 🧪 Тестирование после настройки

### 1. Проверка переменных:

```bash
cd backend
npm run check-cloud-env
```

### 2. Тест IAM токена:

```bash
node -e "require('./services/yandex/iamToken').iamTokenManager.getToken().then(token => console.log('✅ IAM Token получен:', token.substring(0, 20) + '...')).catch(err => console.error('❌ Ошибка:', err.message))"
```

### 3. Тест создания видео:

```bash
# Через API (нужен JWT токен)
curl -X POST http://localhost:3001/api/video-yandex/create \
  -H "Authorization: Bearer YOUR_JWT_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "title": "Test Video",
    "fileSize": 1000000,
    "fileName": "test.mp4",
    "isPublic": true
  }'
```

---

## 🆘 Решение проблем

### Проблема: "IAM token refresh failed"

**Решение:**
1. Проверьте OAuth токен в https://oauth.yandex.ru/
2. Убедитесь, что токен не истек
3. Проверьте, что приложение имеет права на Video Processing

### Проблема: "Video channel not found"

**Решение:**
1. Проверьте Channel ID в Yandex Cloud Console
2. Убедитесь, что канал в правильной папке
3. Проверьте, что сервис Video Processing включен

### Проблема: "Insufficient funds"

**Решение:**
1. Пополните баланс в Yandex Cloud
2. Проверьте лимиты расходов
3. Убедитесь, что платежный метод активен

---

## 📞 Поддержка Yandex Cloud

- **Документация**: https://cloud.yandex.ru/docs/
- **Video Processing**: https://cloud.yandex.ru/docs/video/
- **Техподдержка**: support@cloud.yandex.ru
- **Чат поддержки**: в консоли Yandex Cloud

---

## 🎯 Следующие шаги после настройки

1. ✅ Заполнить все переменные в `.env`
2. ✅ Запустить проверку: `npm run check-cloud-env`
3. ✅ Протестировать загрузку видео
4. ✅ Настроить мониторинг расходов
5. ✅ Начать миграцию с api.video

---

**Важно для Кыргызстана:**
- Используйте регион `ru-central1` для лучшей производительности
- Включите CDN для ускорения доставки
- Настройте уведомления о расходах
- Мониторьте латентность из Кыргызстана

---

**Последнее обновление:** Январь 2025  
**Регион:** Центральная Азия (Кыргызстан)

