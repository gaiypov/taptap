# Полная инструкция по настройке Realtime

## ✅ Что будет работать в реальном времени:

- ✅ **Новые сообщения в чате** → мгновенно у обоих участников
- ✅ **Новый чат** → появляется в списке без pull-to-refresh
- ✅ **Лайки/сохранения** → счётчики обновляются живьём
- ✅ **Объявление продано/удалено** → исчезает у всех
- ✅ **Модерация (бан/разбан)** → пользователь вылетает мгновенно

---

## 📋 Шаг 1: Выполнение SQL миграции в Supabase Dashboard

### Инструкция:

1. **Откройте Supabase Dashboard**
   - Перейдите на https://supabase.com/dashboard
   - Выберите ваш проект

2. **Откройте SQL Editor**
   - В левом меню выберите "SQL Editor"
   - Нажмите "New query"

3. **Выполните миграцию**
   - Откройте файл: `supabase/migrations/20250121_enable_realtime_complete.sql`
   - Скопируйте весь SQL код
   - Вставьте в SQL Editor
   - Нажмите "Run" (или Ctrl+Enter)

4. **Проверьте результат**
   - Должны увидеть сообщения типа: `✅ RLS политики для chat_threads: 3`
   - Если есть предупреждения (⚠️), выполните соответствующие миграции

### Альтернативный способ (через CLI):

```bash
# Если используете Supabase CLI
supabase db push
```

---

## 🔒 Шаг 2: Проверка RLS политик

### Проверка наличия политик:

Выполните в SQL Editor:

```sql
-- Проверка RLS политик для всех таблиц
SELECT 
  tablename,
  COUNT(*) as policy_count,
  STRING_AGG(policyname, ', ') as policies
FROM pg_policies
WHERE tablename IN (
  'chat_threads', 
  'chat_messages', 
  'listings', 
  'listing_likes', 
  'listing_saves'
)
GROUP BY tablename
ORDER BY tablename;
```

### Ожидаемый результат:

- `chat_threads` — минимум 3 политики (SELECT, INSERT, UPDATE)
- `chat_messages` — минимум 3 политики (SELECT, INSERT, UPDATE)
- `listings` — минимум 2 политики (SELECT для всех, SELECT для владельца)
- `listing_likes` — минимум 1 политика (SELECT)
- `listing_saves` — минимум 1 политика (SELECT)

### Если политик нет:

Выполните миграции в порядке:
1. `supabase/migrations/20250102_rls_fixes.sql` — для listings
2. `supabase/migrations/20250120_chat_rls_policies.sql` — для chat

---

## 🔑 Шаг 3: Проверка использования SUPABASE_ANON_KEY

### ✅ Правильно (на клиенте):

**Файл: `services/supabase.ts`**
```typescript
const supabaseAnonKey = 
  Constants.expoConfig?.extra?.EXPO_PUBLIC_SUPABASE_ANON_KEY || 
  process.env.EXPO_PUBLIC_SUPABASE_ANON_KEY || 
  '';

export const supabase = createClient(supabaseUrl, supabaseAnonKey);
```

**Файл: `services/realtime.ts`**
```typescript
import { supabase } from '@/services/supabase'; // ✅ Использует anon key
```

### ❌ Неправильно (на клиенте):

```typescript
// ❌ НИКОГДА не используйте service_role на клиенте!
const supabase = createClient(url, process.env.SUPABASE_SERVICE_ROLE_KEY);
```

### Проверка в коде:

Выполните проверку:

```bash
# Запустите скрипт проверки
chmod +x scripts/check-realtime-setup.sh
./scripts/check-realtime-setup.sh
```

Или проверьте вручную:

```bash
# Поиск использования service_role в клиентском коде
grep -r "SERVICE_ROLE_KEY\|service_role" services/ app/ lib/ --exclude-dir=node_modules

# Должно быть пусто (или только в комментариях)
```

---

## 🧪 Шаг 4: Тестирование Realtime

### Тест 1: Новые сообщения в чате

1. Откройте приложение на двух устройствах/вкладках
2. Авторизуйтесь разными пользователями
3. Откройте один и тот же чат
4. Отправьте сообщение с одного устройства
5. **Ожидаемый результат:** Сообщение должно появиться мгновенно на втором устройстве

### Тест 2: Лайки

1. Откройте объявление
2. Поставьте лайк
3. **Ожидаемый результат:** Счетчик лайков должен обновиться без обновления страницы

### Тест 3: Статус объявления

1. Откройте объявление как продавец
2. Пометьте как "Продано"
3. **Ожидаемый результат:** Объявление должно исчезнуть из ленты у всех пользователей

---

## 🔍 Отладка проблем

### Проблема: Realtime не работает

**Проверьте:**

1. **Миграция выполнена?**
   ```sql
   SELECT tablename FROM pg_publication_tables 
   WHERE pubname = 'supabase_realtime';
   ```

2. **RLS включен?**
   ```sql
   SELECT tablename, rowsecurity 
   FROM pg_tables 
   WHERE tablename IN ('chat_threads', 'chat_messages', 'listings');
   ```

3. **Используется anon key?**
   - Проверьте консоль браузера/приложения
   - Не должно быть ошибок типа "permission denied"
   - Проверьте Network tab — запросы должны идти с anon key

4. **WebSocket подключение?**
   - Откройте DevTools → Network → WS
   - Должно быть активное WebSocket соединение к Supabase

### Проблема: Ошибка "permission denied"

**Причина:** Используется service_role key или нет RLS политик

**Решение:**
1. Убедитесь, что используете `EXPO_PUBLIC_SUPABASE_ANON_KEY`
2. Проверьте, что RLS политики созданы
3. Проверьте, что пользователь авторизован (`auth.uid()` не null)

---

## 📝 Чеклист перед запуском

- [ ] SQL миграция `20250121_enable_realtime_complete.sql` выполнена
- [ ] RLS политики проверены для всех таблиц
- [ ] В клиентском коде используется `EXPO_PUBLIC_SUPABASE_ANON_KEY`
- [ ] В клиентском коде НЕТ `SUPABASE_SERVICE_ROLE_KEY`
- [ ] Redux слайсы (`chatSlice`, `listingsSlice`) добавлены в store
- [ ] Realtime подписки подключены в `app/_layout.tsx`
- [ ] Тестирование пройдено успешно

---

## 🚀 Готово!

После выполнения всех шагов Realtime будет работать автоматически. Все обновления будут приходить мгновенно через WebSocket соединение.

**Важно:** Realtime автоматически уважает RLS политики, поэтому пользователи будут видеть только те данные, к которым у них есть доступ.

