# ⚡ Быстрый старт: ESLint в Cursor

## 🎯 За 2 минуты

### 1. Установите расширение ESLint

В Cursor: `Cmd+Shift+X` → найдите "ESLint" → Install

### 2. Перезапустите Cursor

После установки расширения перезапустите Cursor.

### 3. Проверьте работу

Откройте любой `.ts` файл и создайте неиспользуемую переменную:

```typescript
const test = 123; // Должна появиться красная линия
```

Если видите ошибку — всё работает! ✅

---

## ✅ Что происходит автоматически

- ✅ **При сохранении** (`Cmd+S`) — ESLint автоматически исправляет проблемы
- ✅ **При наборе** — проблемы подсвечиваются в реальном времени
- ✅ **Импорты** — автоматически упорядочиваются
- ✅ **Форматирование** — код автоматически форматируется

---

## 🚨 CI блокирует merge

**Настройка Branch Protection (один раз):**

1. GitHub → Settings → Branches
2. Add Branch Protection Rule
3. Ветки: `main`, `dev`
4. Включите: **Require status checks** → выберите `lint-and-typecheck`

**Результат:** PR не может быть смержен без зеленого статуса ✅

---

## 📋 Команды

```bash
# Проверка
npm run lint

# Строгая проверка (0 warnings)
npm run lint:strict

# Автоисправление
npm run lint:fix

# Проверка сборки
npm run build:check
```

---

**Готово!** ESLint теперь работает в Cursor и блокирует merge в CI.

