# 🔍 Универсальная Система Фильтров - Руководство

## ✅ ЧТО СОЗДАНО

### 1. Конфигурация фильтров
- `config/filterConfig.ts` - Универсальная конфигурация для всех категорий

### 2. UI Компоненты
- `components/Filters/FilterSlider.tsx` - Слайдер диапазона
- `components/Filters/ColorGridFilter.tsx` - Сетка цветов
- `components/Filters/ButtonGroupFilter.tsx` - Группа кнопок
- `components/Filters/SearchableSelectFilter.tsx` - Поиск по списку
- `components/Filters/DualInputFilter.tsx` - Два инпута (для этажей)
- `components/Filters/AdvancedFiltersModal.tsx` - Модал со всеми фильтрами
- `components/Filters/index.ts` - Экспорты

### 3. Категории
- 🚗 **Автомобили** (активно)
- 🐴 **Лошади** (активно)
- 🏠 **Недвижимость** (скоро)

---

## 📦 УСТАНОВКА

### ❌ Внешние зависимости НЕ ТРЕБУЮТСЯ!

Все компоненты созданы с использованием только встроенных React Native компонентов.

### Что уже есть:
```bash
✅ expo-linear-gradient  # Для градиентов
✅ @expo/vector-icons    # Для иконок
```

Никаких дополнительных пакетов устанавливать не нужно!

---

## 🎨 ИСПОЛЬЗОВАНИЕ

### 1. Импорт конфигурации

```typescript
import { 
  FILTER_CONFIG, 
  getCategoryConfig,
  CITIES,
  PRICE_RANGES 
} from '@/config/filterConfig';
```

### 2. Использование в компонентах

```typescript
import { AdvancedFiltersModal } from '@/components/Filters/AdvancedFiltersModal';

function SearchScreen() {
  const [category, setCategory] = useState('auto');
  const [filters, setFilters] = useState({});
  const [showFilters, setShowFilters] = useState(false);
  
  const config = getCategoryConfig(category);
  
  return (
    <>
      {/* Ваш UI */}
      
      <AdvancedFiltersModal
        visible={showFilters}
        category={category}
        filters={filters}
        onApply={(newFilters) => {
          setFilters(newFilters);
          setShowFilters(false);
        }}
        onClose={() => setShowFilters(false)}
        onReset={() => setFilters({})}
        resultsCount={100}
      />
    </>
  );
}
```

---

## 🎯 КОНФИГУРАЦИЯ ФИЛЬТРОВ

### Структура категории:

```typescript
{
  icon: '🚗',
  name: 'Автомобили',
  color: '#E63946',
  
  // Основные фильтры (всегда видны)
  mainFilters: ['city', 'price', 'year'],
  
  // Расширенные фильтры (в модалке)
  advancedFilters: {
    brand: {
      type: 'searchable-select',
      label: '🏭 Марка',
      options: ['Toyota', 'BMW', ...],
      placeholder: 'Поиск марки...'
    },
    // ... другие фильтры
  },
  
  // Переключатели
  toggles: [
    { key: 'verified_only', label: '✓ Только проверенные', default: false }
  ]
}
```

### Типы фильтров:

| Тип | Описание | Пример |
|-----|----------|--------|
| `searchable-select` | Выбор с поиском | Марка авто |
| `select` | Простой выбор | Порода лошади |
| `buttons` | Кнопки выбора | Коробка передач |
| `color-grid` | Сетка цветов | Цвет авто |
| `slider` | Диапазон значений | Цена, пробег |
| `dual-input` | Два поля ввода | Этаж (5 из 9) |

---

## 🚗 АВТОМОБИЛИ

### Основные фильтры:
- Город
- Цена
- Год

### Расширенные:
- Марка (с поиском)
- Модель (зависит от марки)
- Коробка передач
- Тип двигателя
- Цвет
- Пробег (слайдер)
- AI оценка (слайдер)

### Toggles:
- ✓ Только проверенные
- ⭐ С гарантией
- 🤖 С AI анализом

---

## 🐴 ЛОШАДИ

### Основные фильтры:
- Город
- Цена
- Возраст

### Расширенные:
- Порода
- Пол (жеребец/кобыла/мерин)
- Масть
- Рост (слайдер)
- Темперамент

### Toggles:
- ✓ Только проверенные
- 📜 С документами
- 💉 Есть ветпаспорт
- 🏆 Готов к соревнованиям

---

## 🏠 НЕДВИЖИМОСТЬ

### Основные фильтры:
- Город
- Цена
- Тип объекта

### Расширенные:
- Тип (квартира/дом/участок/коммерция)
- Площадь (слайдер)
- Количество комнат
- Этаж (dual-input: "5 из 9")
- Тип здания

### Toggles:
- ✓ Только проверенные
- 📜 Чистые документы
- 🛋️ С мебелью
- 🚗 С парковкой

---

## 🎨 СТИЛИЗАЦИЯ

### Цвета категорий:
```typescript
auto: '#E63946'        // Красный
horse: '#F59E0B'       // Золотой/желтый
real_estate: '#059669' // Зеленый
```

### Темная тема:
```typescript
background: '#000'
surface: '#1C1C1E'
border: '#3A3A3C'
text: '#FFF'
secondary: '#8E8E93'
```

---

## 📝 СЛЕДУЮЩИЕ ШАГИ

### TODO:
1. ✅ Создать filterConfig.ts
2. ✅ Создать UI компоненты
3. ✅ Создать AdvancedFiltersModal
4. ⏳ Обновить SearchScreen.tsx
5. ⏳ Дополнить searchService.ts
6. ⏳ Добавить индексы в БД
7. ⏳ Тестирование

---

## 🔧 КАСТОМИЗАЦИЯ

### Добавить новый фильтр:

```typescript
// В config/filterConfig.ts

advancedFilters: {
  // ... существующие фильтры
  
  // Новый фильтр
  engine_volume: {
    type: 'slider',
    label: '🔧 Объем двигателя',
    min: 1.0,
    max: 6.0,
    step: 0.1,
    unit: 'л'
  }
}
```

### Добавить новую категорию:

```typescript
// В FILTER_CONFIG

my_category: {
  icon: '🎯',
  name: 'Моя категория',
  color: '#FF6B35',
  mainFilters: ['city', 'price'],
  advancedFilters: { /* ... */ },
  toggles: [ /* ... */ ]
}
```

---

## ⚠️ ВАЖНО

### Что НЕ трогать:
- ❌ Существующий SearchScreen.tsx (только дополнять)
- ❌ Существующие API endpoints
- ❌ Структуру базы данных (только добавлять индексы)

### Что можно менять:
- ✅ Добавлять новые фильтры
- ✅ Добавлять новые категории
- ✅ Менять цвета и стили
- ✅ Добавлять новые toggles

---

## 📊 ПРОИЗВОДИТЕЛЬНОСТЬ

### Оптимизации:
- Debounce поиска (500ms)
- Lazy loading опций
- Мемоизация компонентов
- Оптимизированные SQL запросы

---

**Создано:** 20 октября 2025  
**Версия:** 1.0.0  
**Статус:** ✅ Готово к интеграции

