# 🚀 БЫСТРЫЙ СТАРТ - BOOST СИСТЕМА

## ✅ ЧТО ДОБАВЛЕНО В ПРОЕКТ

### 1. База данных
- Таблица `boost_transactions`
- Поля boost в таблице `cars`
- SQL функции и триггеры
- RLS политики

### 2. Платежные системы
- **Mbank** (`services/payments/mbank.ts`)
- **Bakai Bank** (`services/payments/bakaibank.ts`)
- **O!Bank** (`services/payments/obank.ts`)
- **Optima Bank** (`services/payments/optimabank.ts`)

### 3. UI компоненты
- `BoostModal.tsx` - модальное окно выбора тарифа
- `PaymentMethodSelector.tsx` - выбор способа оплаты
- `BoostBadge.tsx` - значок на карточке авто
- `BoostStats.tsx` - статистика эффективности

### 4. Сервисы
- `boost.ts` - логика boost системы
- `webhooks.ts` - обработчики платежей

### 5. Интеграция
- Кнопка BOOST в деталях авто (только для владельца)
- Индикатор активного boost
- Автообновление после оплаты

---

## 📝 ЧТО НУЖНО СДЕЛАТЬ

### Шаг 1: Запустить SQL скрипт

Откройте Supabase SQL Editor и выполните:
```bash
supabase-boost-schema.sql
```

Это создаст:
- Таблицу `boost_transactions`
- Функции `activate_boost`, `has_active_boost`, и др.
- RLS политики
- Views для аналитики

### Шаг 2: Добавить API ключи (опционально)

В `app.json` добавьте (если хотите реальные платежи):

```json
{
  "expo": {
    "extra": {
      "EXPO_PUBLIC_MBANK_API_KEY": "ваш_ключ",
      "EXPO_PUBLIC_MBANK_MERCHANT_ID": "ваш_merchant_id",
      "EXPO_PUBLIC_BAKAI_API_KEY": "ваш_ключ",
      "EXPO_PUBLIC_OBANK_AUTH_TOKEN": "ваш_токен",
      "EXPO_PUBLIC_OBANK_MERCHANT_ID": "ваш_merchant_id",
      "EXPO_PUBLIC_OPTIMA_API_KEY": "ваш_ключ",
      "EXPO_PUBLIC_OPTIMA_MERCHANT_ID": "ваш_merchant_id"
    }
  }
}
```

**Примечание**: Если не добавите ключи, система будет работать в режиме разработки с моковыми данными.

### Шаг 3: Установить зависимости (если нужно)

```bash
npm install
# Все необходимые пакеты уже есть в package.json
```

### Шаг 4: Запустить приложение

```bash
npx expo start
```

Откройте приложение в Expo Go.

### Шаг 5: Протестировать BOOST

1. Откройте любое объявление, владельцем которого вы являетесь
2. Прокрутите вниз до секции "📈 Хотите больше просмотров?"
3. Нажмите "🚀 ПРОДВИНУТЬ ОБЪЯВЛЕНИЕ"
4. Выберите тариф (Базовый, ТОП или Премиум)
5. Выберите способ оплаты
6. Нажмите "Оплатить"
7. В режиме разработки платеж сразу считается успешным
8. Объявление автоматически продвинется!

---

## 🎯 ТАРИФЫ BOOST

| Тариф | Цена | Множитель |
|-------|------|-----------|
| ⭐ Выделение | 50 сом | 2x просмотров |
| 🔥 ТОП | 150 сом | 5x просмотров |
| 💎 Премиум | 300 сом | 10x просмотров |

Все тарифы действуют 24 часа.

---

## 🔧 НАСТРОЙКА PRODUCTION

### 1. Получить API ключи

Свяжитесь с банками:
- **Mbank**: https://mbank.kg (для разработчиков)
- **Bakai Bank**: https://www.bakaibank.kg
- **O!Bank**: https://www.obank.kg
- **Optima Bank**: https://www.optimabank.kg

### 2. Настроить Webhooks

Каждый банк должен отправлять webhook на ваш сервер:

```
POST https://your-api.com/webhooks/mbank
POST https://your-api.com/webhooks/bakai
POST https://your-api.com/webhooks/obank
POST https://your-api.com/webhooks/optima
```

Используйте код из `services/payments/webhooks.ts`

### 3. Настроить Cron Job

Для автоматической деактивации истекших boost:

```sql
-- В Supabase Dashboard → Database → Cron Jobs
-- Запускать каждый час:
SELECT cron.schedule(
  'deactivate-expired-boosts',
  '0 * * * *',
  $$SELECT deactivate_expired_boosts()$$
);
```

Или через Supabase Edge Function (рекомендуется).

---

## 📊 ПРОСМОТР СТАТИСТИКИ

### В приложении
- Откройте свое объявление
- В секции BOOST увидите:
  - Статус (активен/неактивен)
  - Оставшееся время
  - Тип boost

### В Supabase SQL Editor

```sql
-- Все транзакции
SELECT * FROM boost_transactions 
ORDER BY created_at DESC;

-- Активные boost
SELECT * FROM active_boosts;

-- Статистика за месяц
SELECT * FROM get_boost_stats(
  NOW() - INTERVAL '30 days',
  NOW()
);

-- Доход по дням
SELECT * FROM daily_boost_revenue
LIMIT 30;
```

---

## 🐛 TROUBLESHOOTING

### Кнопка BOOST не появляется
- Убедитесь что вы владелец объявления
- Проверьте что `currentUser.id === car.seller_id`

### Платеж не создается
- Проверьте консоль браузера/Expo на ошибки
- Убедитесь что SQL скрипт выполнен
- Проверьте RLS политики

### Boost не активируется автоматически
- В режиме разработки платеж моковый, но boost должен активироваться
- Проверьте функцию `activate_boost` в БД
- Проверьте логи

---

## 📖 ДОПОЛНИТЕЛЬНАЯ ДОКУМЕНТАЦИЯ

- **BOOST_MONETIZATION_GUIDE.md** - полный гайд по boost системе
- **PROJECT_FINAL_SUMMARY.md** - итоговое резюме всего проекта
- **README.md** - общая информация о проекте

---

## ✅ ЧЕКЛИСТ ЗАПУСКА

- [ ] SQL скрипт выполнен
- [ ] Приложение запускается без ошибок
- [ ] Кнопка BOOST появляется у владельца
- [ ] Модалка выбора тарифа открывается
- [ ] Можно выбрать способ оплаты
- [ ] После "оплаты" boost активируется
- [ ] Бейдж boost отображается на объявлении
- [ ] Счетчик часов работает

---

## 🎉 ВСЁ ГОТОВО!

Система BOOST полностью функциональна и готова к использованию!

**Следующие шаги:**
1. Протестировать все функции
2. Получить реальные API ключи от банков
3. Настроить webhooks
4. Запустить в production
5. Начать зарабатывать! 💰

**Удачи с запуском!** 🚀

