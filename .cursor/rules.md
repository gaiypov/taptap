# 360° Marketplace — Правила разработки

> **Лучшее приложение Кыргызстана 2025–2026**  
> Staff+ Fullstack инженер из Бишкека  
> Версия: ноябрь 2025, 10k+ пользователей

---

## 🛠 Стек технологий

- **Expo 51+** (Hermes, React Compiler)
- **expo-video** (НЕ expo-camera!)
- **Redux Toolkit** + typed hooks
- **Supabase** (RLS-only, anon_key на фронте, НИКОГДА service_role на бэкенде)
- **Nikita.kg SMS**
- **api.video** delegated upload
- **Reels как в TikTok** (видео, лайк по двойному тапу, кнопки справа)

---

## ⚠️ КРИТИЧЕСКИ ВАЖНЫЕ ПРАВИЛА

> **ВНИМАНИЕ:** Нарушение этих правил приводит к падению приложения в Expo Go!

### 1. Reels — только Pressable
```tsx
// ✅ ПРАВИЛЬНО
<Pressable
  onPress={(e) => {
    const x = e.nativeEvent?.locationX ?? e.nativeEvent?.pageX ?? 0;
    if (x > SCREEN_WIDTH * 0.7) return; // правая панель — игнор
    handleDoubleTap();
  }}
>

// ❌ НИКОГДА TouchableWithoutFeedback!
```

### 2. OptimizedVideoPlayer — player из useRef
```tsx
// ✅ ПРАВИЛЬНО
const player = useRef(useVideoPlayer(videoUrl, player => {
  player.loop = true;
  player.muted = isMuted;
})).current;

// ❌ НИКОГДА player.release() в cleanup!
// Плеер живёт весь сеанс скроллинга
```

### 3. Камера — только expo-video
```tsx
// ✅ ПРАВИЛЬНО
const [isCameraReady, setIsCameraReady] = useState(false);

<CameraView
  onCameraReady={() => setIsCameraReady(true)}
/>

<TouchableOpacity
  disabled={!isCameraReady}
  onPress={() => {
    if (isCameraReady) {
      cameraRef.current?.startRecording();
    }
  }}
>

// ❌ НИКОГДА startRecording() без onCameraReady!
```

### 4. SMS — всегда testCode в development
```tsx
// ✅ ПРАВИЛЬНО
if (__DEV__ && !process.env.NIKITA_API_KEY) {
  return { success: true, testCode: '111111' };
}
```

### 5. Redux — хуки только из '@/lib/store/hooks'
```tsx
// ✅ ПРАВИЛЬНО
import { useAppDispatch, useAppSelector } from '@/lib/store/hooks';

const dispatch = useAppDispatch();
const data = useAppSelector(state => state.something);

// ❌ НИКОГДА прямые импорты из react-redux!
```

### 6. Supabase — join'ы только через FK
```sql
-- ✅ ПРАВИЛЬНО
SELECT l.*, p.name, p.avatar_url
FROM listings l
JOIN profiles p ON l.seller_id = p.id;

-- ❌ НИКОГДА без FK или через users вместо profiles!
```

### 7. Видео — только delegated upload
```tsx
// ✅ ПРАВИЛЬНО
const uploadToken = await apiVideo.createDelegatedUploadToken();
await apiVideo.uploadVideo(file, uploadToken);

// ❌ НИКОГДА через бэкенд!
```

### 8. Consents — версии в .env
```tsx
// ✅ ПРАВИЛЬНО
const CONSENT_VERSION = process.env.CONSENT_VERSION || '1.0.0';

// Проверка версии при каждом запуске
// Reconsent при смене версии
// Audit log всех согласий
```

### 9. Типизация — НИКОГДА any
```tsx
// ✅ ПРАВИЛЬНО
details?: Record<string, unknown>;
listing: Listing & { category?: string };

// ❌ НИКОГДА any!
```

### 10. Перед изменением — объясни что и почему
> Всегда кратко объясняй изменения перед их внесением.

---

## 📐 Правила качества

### Производительность
- ✅ Работает на **Beeline 3G в Оше**
- ✅ Видео играет **мгновенно**, превью показывается **сразу**
- ✅ Кнопки справа — **кликабельны**
- ✅ Лайк по двойному тапу + **анимация**
- ✅ Звук **переключается**

### Функциональность
- ✅ Запись видео — **без ошибки "Camera is not ready"**
- ✅ SMS — **всегда проходит в dev** (testCode: '111111')
- ✅ Supabase — **без PGRST200** (правильные join'ы)

### UX
- ✅ Плавный скролл без лагов
- ✅ Мгновенная реакция на тапы
- ✅ Красивые анимации (LikeAnimation)
- ✅ Haptic feedback на всех действиях

---

## 📁 Структура проекта

```
360AutoMVP/
├── app/                    # Expo Router pages
│   ├── (tabs)/            # Main tabs
│   ├── (auth)/            # Auth screens
│   └── (onboarding)/      # Onboarding
├── components/            # Reusable UI components
│   ├── VideoFeed/         # Reels components
│   └── animations/        # LikeAnimation, etc.
├── services/              # API clients
│   ├── supabase.ts        # Supabase client (anon_key)
│   ├── apiVideo.ts        # api.video delegated upload
│   └── smsService.ts      # Nikita.kg SMS
├── lib/store/             # Redux store
│   ├── hooks.ts           # Typed hooks
│   └── slices/            # Redux slices
├── backend/               # Express API
│   ├── src/api/v1/        # API routes
│   └── services/          # Business logic
└── supabase/              # SQL migrations
    └── migrations/        # RLS policies, Realtime
```

---

## 🔧 Типичные задачи

### «Почини видеофид полностью»
- Проверить `Pressable` вместо `TouchableWithoutFeedback`
- Убедиться что `player.release()` удалён
- Проверить `useAppDispatch`/`useAppSelector`
- Убедиться что превью скрывается после загрузки
- Проверить что кнопки справа работают (locationX > 0.7)

### «Сделай запись видео как в TikTok»
- Использовать `expo-video` CameraView
- `onCameraReady` + `disabled` на кнопке
- `startRecording()` только после ready
- Обработка ошибок "Camera is not ready"

### «Почини SMS, чтобы в dev всегда был код 111111»
- Проверить `__DEV__` флаг
- Вернуть `testCode: '111111'` если нет `NIKITA_API_KEY`
- Логировать в development режиме

### «Почини ошибку PGRST200 в listings»
- Проверить join'ы через `profiles!seller_id`
- Убедиться что FK существует
- Использовать правильные имена таблиц (`profiles`, не `users`)
- Проверить RLS политики

---

## 🚨 Частые ошибки

1. **TouchableWithoutFeedback в Reels** → заменить на `Pressable`
2. **player.release()** → удалить навсегда
3. **startRecording() без onCameraReady** → добавить проверку
4. **any в типах** → заменить на `Record<string, unknown>` или конкретный тип
5. **Прямые импорты из react-redux** → использовать `@/lib/store/hooks`
6. **service_role на фронте** → только `anon_key` + RLS
7. **Join без FK** → добавить FK или использовать правильные таблицы

---

## 📝 Примечания

- Все изменения должны работать на **Expo Go** (iOS + Android)
- Тестировать на **медленном интернете** (Beeline 3G)
- Все ошибки линтера должны быть исправлены
- Код должен быть **читаемым** и **типизированным**

---

---

## 🎨 REVOLUT ULTRA PLATINUM 2025-2026 — ДИЗАЙН СИСТЕМА

> **Ты — дизайнер и React Native инженер уровня Revolut Ultra 2025–2026.**

### Сделай ВСЁ приложение 360° Marketplace в стиле Revolut Ultra Platinum:

**Цветовая палитра:**
- Фон: `#0D0D0D` (глубокий космос-чёрный)
- Карточки: `#171717` (тёмная платина)
- Панели: `#1E1E1E`
- Текст основной: `#FFFFFF`
- Текст второстепенный: `#B8B8B8`
- Текст приглушённый: `#707070`
- Акценты: серебро `#C0C0C0` и `#E0E0E0`
- Градиенты: `#2C2C2C → #1A1A1E`
- Обводки: `#2A2A2A`
- **НИКАКОГО КРАСНОГО ЦВЕТА!**

**Используй тему из `@/lib/theme/ultra`:**
```tsx
import { ultra } from '@/lib/theme/ultra';
// Используй ultra.background, ultra.card, ultra.textPrimary и т.д.
```

**ОБЯЗАТЕЛЬНО:**

1. **Шапка сверху:** текст "Авто   Лошади   Недвижимость" (полупрозрачный фон с блюром `BlurView`)
   - Шрифт: bold, 18px
   - Активная вкладка: белый текст с белой подчеркивающей полоской 3px
   - Неактивные: `#707070`

2. **Нижняя карточка в Reels** — стекло с блюром (`expo-blur`, `intensity={20}`)
   - `borderRadius: 28`
   - `borderWidth: 1`, `borderColor: 'rgba(255,255,255,0.08)'`
   - Цена: большой белый текст `#E0E0E0` с лёгким свечением, `fontSize: 38`, `fontWeight: '900'`

3. **Кнопки справа** — полупрозрачные круги с белой обводкой
   - `width: 64`, `height: 64`, `borderRadius: 32`
   - `backgroundColor: 'rgba(255,255,255,0.08)'`
   - `borderWidth: 1.5`, `borderColor: 'rgba(255,255,255,0.15)'`
   - Активные: `backgroundColor: 'rgba(255,255,255,0.2)'`, `borderColor: '#FFFFFF'`

4. **Плавающая кнопка "+"** — градиент металлик `#2C2C2C → #1A1A1E`
   - `borderWidth: 2`, `borderColor: '#333333'`
   - Тонкая белая обводка

5. **Таббар снизу** — чёрный матовый `#0D0D0D`
   - Иконки: серебро `#C0C0C0` (активные), `#707070` (неактивные)
   - Без бордера сверху

**НИЧЕГО НЕ ЛОМАЙ:**
- Reels, видео, камера, SMS, чаты, Redux — всё должно работать
- Только меняй цвета, стили, шрифты, блюр
- НЕ трогай логику: `onCameraReady`, `Pressable`, `pointerEvents`, `useAppDispatch`/`useAppSelector`

**Пройдись по всему проекту и сделай ВСЁ в этом стиле. Не спрашивай — делай сразу.**

---

**Последнее обновление:** ноябрь 2025  
**Версия приложения:** 1.0.0  
**Пользователей:** 10k+
