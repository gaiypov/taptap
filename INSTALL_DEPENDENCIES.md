# 📦 Установка Зависимостей

## ⚠️ ВАЖНО: Установите expo-blur

Для работы CategorySelectionModal требуется `expo-blur`.

### **Быстрая Установка:**

```bash
npx expo install expo-blur
```

### **После Установки:**

```bash
# Перезапустите приложение
npx expo start --clear
```

---

## ✅ Проверка Установленных Пакетов

Убедитесь, что установлены все необходимые пакеты:

```bash
npx expo install expo-blur expo-linear-gradient expo-camera expo-file-system
```

---

## 🎯 Что Устанавливаем

| Пакет | Назначение | Статус |
|-------|------------|--------|
| `expo-blur` | Blur эффекты в модале | ⚠️ Нужно установить |
| `expo-linear-gradient` | Градиенты | ✅ Уже есть |
| `expo-camera` | Запись видео | ✅ Уже есть |
| `expo-file-system` | Работа с файлами | ✅ Уже есть |

---

## 🐛 Если Ошибки

### **Ошибка: "expo-blur not found"**

```bash
npx expo install expo-blur
npx expo start --clear
```

### **Ошибка: "Invariant Violation"**

```bash
rm -rf node_modules
npm install
npx expo start --clear
```

### **Ошибка: "Metro bundler error"**

```bash
npx expo start --reset-cache
```

---

## 🚀 После Установки

1. Перезапустите Metro bundler
2. Откройте вкладку "Загрузить"
3. Нажмите "Записать видео"
4. Модал должен открыться с blur эффектом

---

**Статус:** ⚠️ Требуется действие - установите expo-blur

