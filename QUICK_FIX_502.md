# 🚨 БЫСТРОЕ ИСПРАВЛЕНИЕ ОШИБКИ 502

## Проблема
```
{"status":502,"statusText":"","error":{}}
```
**Бэкенд не отвечает!**

## Решение за 2 минуты

### 1. Запустите бэкенд
```bash
cd backend
npm run dev
```

Должно появиться:
```
🚀 360⁰ Marketplace API Server Started
🌐 Listening on port: 3001
```

### 2. Проверьте IP в app.json

**ВАЖНО:** На телефоне `localhost` не работает!

1. Узнайте свой IP:
   - **Mac**: `ifconfig | grep "inet " | grep -v 127.0.0.1`
   - **Windows**: `ipconfig` → IPv4 адрес
   - Пример: `192.168.1.16`

2. Откройте `app.json`:
```json
{
  "expo": {
    "extra": {
      "apiUrl": "http://192.168.1.16:3001/api"  // ← ВАШ IP!
    }
  }
}
```

3. Перезапустите Expo:
```bash
# Ctrl+C чтобы остановить
npx expo start --clear
```

### 3. Проверьте
```bash
curl http://192.168.1.16:3001/health
```

Должен вернуться: `{"status":"healthy"}`

## ✅ Готово!

Теперь SMS должны работать.

## 📖 Подробная инструкция
См. [docs/FIX_502_BACKEND_ERROR.md](docs/FIX_502_BACKEND_ERROR.md)

