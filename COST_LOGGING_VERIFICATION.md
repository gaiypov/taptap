# 💰 Проверка логов расходов AI

## ✅ Логи расходов работают корректно!

### 📊 Результаты тестирования:

```
🧪 Testing AI Cost Logging...
========================

Testing Claude costs:
💰 AI Cost: $0.0230 (claude, 1 images)
💰 AI Cost: $0.0690 (claude, 3 images)

Testing OpenAI costs:
💰 AI Cost: $0.0300 (openai, 1 images)
💰 AI Cost: $0.0900 (openai, 3 images)

Testing Google costs:
💰 AI Cost: $0.0000 (google, 1 images)
💰 AI Cost: $0.0000 (google, 1000 images)

Testing Mock costs:
💰 AI Cost: $0.0000 (mock, 1 images)
```

## 🔧 Как проверить логи в приложении:

### 1. **Используйте тестовый компонент:**
```typescript
import CostLoggerTest from '@/components/CostLoggerTest';

// В любом экране
<CostLoggerTest />
```

### 2. **Или используйте тестовый экран:**
Перейдите на `/test-costs` в приложении

### 3. **Проверьте консоль Expo:**
```bash
npx expo start
# Откройте приложение и используйте AI функции
# Логи появятся в консоли Expo
```

## 📈 Анализ расходов:

### **Claude (Приоритетный):**
- **1 изображение**: $0.0230
- **3 изображения**: $0.0690
- **Самый дешевый** провайдер

### **OpenAI (Резервный):**
- **1 изображение**: $0.0300
- **3 изображения**: $0.0900
- **На 30% дороже** Claude

### **Google Vision:**
- **1 изображение**: $0.0000 (бесплатно)
- **1000+ изображений**: $0.0015 за изображение
- **Бесплатно** для небольших объемов

### **Mock (Тестирование):**
- **Любое количество**: $0.0000
- **Полностью бесплатно**

## 🧪 Тестовый режим экономии:

### **Настройки TEST_CONFIG:**
```typescript
export const TEST_CONFIG = {
  USE_SINGLE_IMAGE: true,        // Только 1 изображение (экономия 66%)
  CACHE_RESULTS: true,           // Кеширование результатов
  MAX_REQUESTS_PER_DAY: 20,      // Лимит запросов
  ENABLE_CLAUDE: true,          // Главный AI (самый дешевый)
  ENABLE_OPENAI: false,         // Резервный (дороже)
  ENABLE_GOOGLE: true,          // OCR (бесплатно)
  ENABLE_YOLO: false,           // Пропускаем для MVP
};
```

### **Экономия в тестовом режиме:**
- **Обычный режим**: 3 изображения × $0.023 = $0.069
- **Тестовый режим**: 1 изображение × $0.023 = $0.023
- **Экономия**: 66% на каждом запросе

## 🔍 Где смотреть логи:

### **1. Консоль Expo:**
```bash
npx expo start
# Логи появятся в терминале
```

### **2. Консоль браузера (Web):**
```bash
# Откройте DevTools → Console
```

### **3. React Native Debugger:**
```bash
# Установите React Native Debugger
# Логи появятся в Debugger
```

### **4. Компонент CostLoggerTest:**
```typescript
// Встроенные логи в приложении
<CostLoggerTest />
```

## 📊 Мониторинг расходов:

### **Автоматическое логирование:**
```typescript
// В development режиме автоматически логируется:
logAPICost('claude', 3); // $0.069 (claude, 3 images)
```

### **Счетчик запросов:**
```typescript
// Отслеживание лимитов
console.log(`📊 Requests today: ${requestCount}/${REQUEST_LIMIT}`);
```

### **Статус AI провайдеров:**
```typescript
// Проверка доступности
const keys = checkAPIKeys();
console.log('🔑 Available APIs:', keys);
```

## ⚠️ Важные моменты:

### **1. Логи только в development:**
```typescript
if (AI_CONFIG.MODE !== 'development') return;
```

### **2. Production режим:**
- Логи отключены для производительности
- Используйте внешний мониторинг

### **3. Безопасность:**
- API ключи НЕ логируются
- Только стоимость запросов

## 🎯 Рекомендации:

### **Для разработки:**
- Используйте тестовый режим
- Мониторьте расходы в консоли
- Тестируйте с разными провайдерами

### **Для продакшена:**
- Отключите логирование
- Используйте внешний мониторинг
- Настройте алерты на расходы

---

**✅ Логи расходов работают корректно и готовы к использованию!**
