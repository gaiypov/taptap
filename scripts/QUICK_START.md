# 🚀 Быстрый старт - Загрузка тестовых видео

## Вариант 1: Автоматический (рекомендуется)

```bash
bash scripts/setup-and-seed.sh
```

Скрипт автоматически:
1. Создаст `.env` файл если его нет
2. Проверит наличие SERVICE_ROLE_KEY
3. Запустит загрузку видео

## Вариант 2: Вручную

### Шаг 1: Получите SERVICE_ROLE_KEY

1. Откройте: **https://supabase.com/dashboard/project/thqlfkngyipdscckbhor/settings/api**
2. Найдите раздел **"Project API keys"**
3. Скопируйте ключ из строки **"service_role"** (НЕ "anon"!)

### Шаг 2: Создайте .env файл

```bash
cat > .env << 'EOF'
EXPO_PUBLIC_SUPABASE_URL=https://thqlfkngyipdscckbhor.supabase.co
SUPABASE_SERVICE_ROLE_KEY=ваш-скопированный-ключ-здесь
EOF
```

Или добавьте в `app.json`:
```json
{
  "expo": {
    "extra": {
      "SUPABASE_SERVICE_ROLE_KEY": "ваш-ключ"
    }
  }
}
```

### Шаг 3: Запустите скрипт

```bash
npm run seed:videos
```

## Результат

После успешного выполнения будет загружено 9 тестовых видео:
- ✅ 3 автомобиля
- ✅ 3 лошади  
- ✅ 3 недвижимости

Все видео будут сразу активны и появятся на главной странице!

