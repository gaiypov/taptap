# 📹 Установка API Video ключа

## Что такое API Video?

API Video — это сервис для хостинга и стриминга видео. Все видео в приложении загружаются на api.video и воспроизводятся через HLS стриминг.

## 🚀 Быстрая установка (3 минуты)

### Шаг 1: Получите API ключ

1. Откройте <https://dashboard.api.video/>
2. Зарегистрируйтесь или войдите в аккаунт
3. Перейдите в раздел **"API Keys"** (в меню слева)
4. Скопируйте ваш **API ключ** (начинается с `vi...`)

### Шаг 2: Добавьте ключ в `.env`

Откройте файл `backend/.env` и добавьте (или замените):

```env
# API Video Service (api.video)
APIVIDEO_API_KEY=ваш_реальный_ключ_здесь
```

**Или используйте альтернативное имя:**
```env
API_VIDEO_KEY=ваш_реальный_ключ_здесь
```

**ВАЖНО:**
- Замените `ваш_реальный_ключ_здесь` на реальный ключ из dashboard
- Не используйте кавычки вокруг ключа
- Ключ должен быть на одной строке

### Шаг 3: Перезапустите бэкенд

```bash
cd backend
# Остановите текущий процесс (Ctrl+C)
npm run dev
```

## ✅ Проверка установки

### Способ 1: Автоматическая проверка

```bash
cd backend
npm run check-env
```

Скрипт покажет статус всех переменных окружения, включая `APIVIDEO_API_KEY`.

### Способ 2: Тест API

```bash
curl -X POST http://localhost:3001/api/video/create \
  -H "Content-Type: application/json" \
  -d '{"title":"Test Video"}'
```

**Ожидаемый ответ:**
```json
{
  "success": true,
  "videoId": "vi...",
  "uploadToken": "tok_..."
}
```

**Если ошибка:**
- Проверьте, что ключ правильный
- Убедитесь, что бэкенд перезапущен после изменения `.env`
- Проверьте логи бэкенда на наличие ошибок

## 🔍 Где используется API Video ключ?

### В бэкенде:

1. **`backend/api/video.ts`** — создание видео и получение upload token
2. **`backend/services/apiVideoService.ts`** — загрузка видео и управление

### На клиенте:

- **`services/apiVideo.ts`** — загрузка видео с клиента (delegated upload)
- Видео загружаются напрямую с телефона на api.video (без нагрузки на ваш сервер)

## 📋 Пример полного `.env` файла

```env
# Supabase
SUPABASE_URL=https://thqlfkngyipdscckbhor.supabase.co
SUPABASE_SERVICE_ROLE_KEY=ваш_service_role_ключ

# SMS
SMS_LOGIN=superapp
SMS_PASSWORD=ваш_пароль
SMS_SENDER=bat-bat.kg
SMS_API_URL=https://smspro.nikita.kg/api/message

# API Video
APIVIDEO_API_KEY=ваш_api_video_ключ

# Server
PORT=3001
NODE_ENV=production
```

## ❌ Частые ошибки

### "API_VIDEO_KEY is required"

- Убедитесь, что ключ добавлен в `backend/.env`
- Проверьте, что нет опечаток в имени переменной
- Перезапустите бэкенд после изменения `.env`

### "Invalid API key" или "Unauthorized"

- Проверьте, что ключ скопирован полностью (без пробелов)
- Убедитесь, что используете правильный ключ из dashboard
- Проверьте, что аккаунт api.video активен

### "Failed to create video"

- Проверьте интернет-соединение
- Убедитесь, что api.video доступен (нет блокировки)
- Проверьте баланс на аккаунте api.video (если есть лимиты)

## 💡 Полезные ссылки

- **Dashboard:** <https://dashboard.api.video/>
- **Документация:** <https://docs.api.video/>
- **API Reference:** <https://docs.api.video/5.1/reference>

## 🎯 После установки

После успешной установки ключа:
- ✅ Видео будут загружаться на api.video
- ✅ HLS URL будут генерироваться автоматически
- ✅ Видео будут доступны для воспроизведения в приложении
- ✅ Миниатюры будут создаваться автоматически

