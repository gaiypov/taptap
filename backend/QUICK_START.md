# 🚀 Быстрый запуск бэкенда

## Проблема: Ошибка 502 Bad Gateway

Это означает, что бэкенд сервер не запущен.

## Решение за 3 шага:

### 1. Проверьте `.env` файл

Убедитесь, что в `backend/.env` есть реальные ключи (не `your-...`):

```env
SUPABASE_URL=https://thqlfkngyipdscckbhor.supabase.co
SUPABASE_SERVICE_ROLE_KEY=реальный-ключ-здесь
SUPABASE_ANON_KEY=реальный-ключ-здесь
JWT_SECRET=любой-секретный-ключ-минимум-32-символа
```

### 2. Запустите сервер

```bash
cd backend
npm run dev
```

Должны увидеть:
```
🚀 360⁰ Marketplace API Server Started
🌐 Listening on port: 3001
```

### 3. Проверьте работу

В другом терминале:
```bash
curl http://localhost:3001/api/auth/sms-status
```

Должен вернуть JSON с `success: true`.

## Если не работает:

1. **Проверьте порт**: `lsof -i :3001`
2. **Убейте процесс**: `lsof -ti:3001 | xargs kill -9`
3. **Проверьте логи**: смотрите вывод `npm run dev`

## Важно:

- В `app.json` должен быть правильный IP: `http://192.168.1.16:3001/api`
- Если ваш IP другой, измените в `app.json`

