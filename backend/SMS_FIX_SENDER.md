# 🔧 ИСПРАВЛЕНИЕ: SMS не приходит на nikita.kg

## ✅ Что исправлено:

1. **Приоритет Sender ID:** Теперь используется `SMS_SENDER` (bat-bat.kg) в приоритете над `NIKITA_SMS_SENDER` (360Market)
2. **Детальное логирование:** Добавлено логирование полного XML запроса и ответа от nikita.kg
3. **Логирование sender:** В логах видно, какой sender используется

## 📋 Что нужно проверить:

### 1. Проверьте, что `bat-bat.kg` зарегистрирован в nikita.kg

1. Зайдите на https://smspro.nikita.kg
2. Перейдите в раздел "Отправители" или "Sender ID"
3. Убедитесь, что `bat-bat.kg` подтвержден модератором
4. Если нет - подайте заявку на регистрацию

### 2. Проверьте баланс

1. Зайдите на https://smspro.nikita.kg
2. Проверьте баланс на счету
3. Пополните счет при необходимости

### 3. Проверьте логи бэкенда

После перезапуска бэкенда попробуйте отправить SMS и проверьте логи:

```bash
tail -f /tmp/backend.log | grep SMS
```

Должны быть:
```
[SMS] Sending request to nikita.kg: {
  url: 'https://smspro.nikita.kg/api/message',
  login: 'superapp',
  sender: 'bat-bat.kg',  // ← Должен быть bat-bat.kg, не 360Market!
  phone: '996779728888',
  ...
}
[SMS] Response from nikita.kg: {
  status: 200,
  fullResponse: '<?xml version="1.0"...',  // ← Полный ответ
  ...
}
```

### 4. Проверьте переменные окружения

```bash
cd backend
cat .env | grep SMS_SENDER
```

Должно быть:
```bash
SMS_SENDER=bat-bat.kg
```

## 🔍 Возможные причины, почему SMS не приходит:

1. **Sender ID не подтвержден** - `bat-bat.kg` должен быть подтвержден в личном кабинете
2. **Недостаточно средств** - проверьте баланс на nikita.kg
3. **Неправильные учетные данные** - проверьте `SMS_LOGIN` и `SMS_PASSWORD`
4. **Проблема с API nikita.kg** - свяжитесь с поддержкой

## 📞 Контакты поддержки nikita.kg

- **Телефон:** +996 (312) 439217 или +996 (779) 377888
- **Email:** support@nikita.kg
- **Сайт:** https://nikita.kg

## ✅ Следующие шаги:

1. Перезапустите бэкенд
2. Проверьте логи - там будет видно, какой sender используется
3. Проверьте личный кабинет nikita.kg - убедитесь, что `bat-bat.kg` подтвержден
4. Проверьте баланс - пополните счет при необходимости
5. Если проблема сохраняется - свяжитесь с поддержкой nikita.kg

