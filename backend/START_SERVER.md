# 🚀 Как запустить бэкенд сервер

## Быстрый старт

```bash
cd backend
npm run dev
```

## Проверка работы

После запуска проверьте:

```bash
curl http://localhost:3001/health
```

Должен вернуть:
```json
{
  "success": true,
  "data": {
    "status": "healthy",
    "timestamp": "...",
    "uptime": ...,
    "environment": "production",
    "version": "1.0.0"
  }
}
```

## Проверка SMS endpoint

```bash
curl http://localhost:3001/api/auth/sms-status
```

## Проблема 502 Bad Gateway

**Причина:** Бэкенд не запущен или недоступен

**Решение:**
1. Запустите бэкенд: `cd backend && npm run dev`
2. Проверьте, что порт 3001 свободен: `lsof -ti:3001`
3. Проверьте IP адрес в `app.json` - должен совпадать с вашим IP
4. Проверьте доступность: `curl http://localhost:3001/health`

## Проверка IP адреса

В `app.json` указан IP: `http://192.168.1.16:3001/api`

Проверьте ваш текущий IP:
```bash
ifconfig | grep "inet.*192.168"
```

Если IP изменился, обновите `app.json`:
```json
{
  "extra": {
    "apiUrl": "http://ВАШ_IP:3001/api"
  }
}
```

## Автозапуск при старте системы

Для production используйте PM2:

```bash
npm install -g pm2
cd backend
pm2 start npm --name "360-backend" -- run dev
pm2 save
pm2 startup
```
