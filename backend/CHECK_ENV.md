# 🔍 Проверка переменных окружения

## Как убедиться, что APIVIDEO_API_KEY настроен

### Способ 1: Автоматическая проверка (рекомендуется)

Запустите скрипт проверки:

```bash
cd backend
npm run check-env
```

Скрипт проверит:
- ✅ Существование файла `.env`
- ✅ Наличие всех обязательных переменных
- ✅ Правильность их значений

### Способ 2: Ручная проверка

1. **Проверьте наличие файла `.env` в папке `backend/`:**

```bash
cd backend
ls -la .env
```

Если файла нет, создайте его:

```bash
cp env-config.txt .env
```

2. **Откройте файл `.env` и проверьте наличие:**

```env
APIVIDEO_API_KEY=your_actual_api_key_here
```

или

```env
API_VIDEO_KEY=your_actual_api_key_here
```

3. **Убедитесь, что значение НЕ содержит:**
   - `your-`
   - `change-this`
   - Пустое значение

### Способ 3: Проверка при запуске сервера

При запуске бэкенда вы увидите предупреждение, если ключ не настроен:

```bash
npm run dev
```

Если увидите:
```
❌ APIVIDEO_API_KEY не задан в .env файле!
   Добавьте в backend/.env:
   APIVIDEO_API_KEY=your_api_key_here
```

Значит нужно добавить ключ в `.env`.

## Где получить API ключ

1. Зарегистрируйтесь на [api.video](https://dashboard.api.video/)
2. Перейдите в Settings → API Keys
3. Скопируйте ваш API ключ
4. Добавьте в `backend/.env`:

```env
APIVIDEO_API_KEY=ваш_ключ_здесь
```

## Структура файла .env

Минимальный `.env` файл должен содержать:

```env
# API Video Service
APIVIDEO_API_KEY=ваш_ключ_здесь

# JWT
JWT_SECRET=ваш_jwt_secret

# Supabase
SUPABASE_URL=ваш_supabase_url
SUPABASE_SERVICE_ROLE_KEY=ваш_service_role_key
```

Полный пример смотрите в `backend/env-config.txt`.

## Важно

- ⚠️ **НЕ коммитьте `.env` файл в Git!** (он уже в `.gitignore`)
- ✅ Используйте `env-config.txt` как шаблон
- ✅ В production используйте переменные окружения сервера
- ✅ Регулярно проверяйте конфигурацию командой `npm run check-env`

