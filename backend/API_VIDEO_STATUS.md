# 📹 API.VIDEO - Статус конфигурации

## ❌ Проблема: API ключ не настроен

В `backend/.env` установлен placeholder:
```bash
API_VIDEO_KEY=your-api-video-key
```

## ✅ Что нужно сделать:

### 1. Получите API ключ от api.video

1. Зайдите на https://dashboard.api.video/
2. Зарегистрируйтесь или войдите
3. Перейдите в раздел "API Keys"
4. Скопируйте ваш API ключ

### 2. Установите ключ в `.env`

Откройте `backend/.env` и замените:
```bash
API_VIDEO_KEY=your-api-video-key
```

На:
```bash
API_VIDEO_KEY=ваш_реальный_ключ_от_api_video
```

Или используйте:
```bash
APIVIDEO_API_KEY=ваш_реальный_ключ_от_api_video
```

### 3. Перезапустите бэкенд

```bash
cd backend
npm run dev
```

## 🔍 Проверка доступности api.video

После установки ключа проверьте:

```bash
curl -X POST http://localhost:3001/api/video/create \
  -H "Content-Type: application/json" \
  -d '{"title":"Test Video"}'
```

**Ожидаемый ответ:**
```json
{
  "success": true,
  "videoId": "vi...",
  "uploadToken": "tok_..."
}
```

**Если ошибка:**
- Проверьте, что ключ правильный
- Проверьте, что api.video доступен (нет блокировки сети)
- Проверьте логи бэкенда

## 📋 Текущая конфигурация:

- ✅ Маршрут `/api/video/create` настроен
- ✅ Код для delegated upload готов
- ❌ API ключ не установлен (placeholder)

## 🚀 После настройки:

1. Видео будут загружаться на api.video
2. HLS URL будут генерироваться автоматически
3. Видео будут доступны для воспроизведения

