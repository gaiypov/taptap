# 🚀 Быстрый запуск приложения

## ✅ Что уже исправлено:

1. ✅ Удалено устаревшее поле `expoClient` из `app.json`
2. ✅ Исправлены экспорты в `lib/theme/index.tsx` (ThemedBackground, useAppTheme, useThemeContext)
3. ✅ Исправлена ошибка с типами в `OptimizedVideoPlayer.tsx`
4. ✅ Очищен кэш `.expo`

## 🔧 Как запустить приложение:

### 1. Остановите все процессы Metro/Expo:
```bash
killall node
# или
lsof -ti:8081 | xargs kill -9
```

### 2. Очистите кэш и перезапустите:
```bash
# Очистка кэша
rm -rf .expo .expo-shared node_modules/.cache

# Запуск с очисткой кэша
npx expo start --clear
```

### 3. Альтернативный способ (автоматический скрипт):
```bash
npm run start:auto
# или
./scripts/start-auto.sh
```

## 📱 Подключение устройства:

### iOS Simulator:
```bash
# После запуска Metro нажмите 'i' в терминале
# или
npx expo start --ios
```

### Android Emulator:
```bash
# После запуска Metro нажмите 'a' в терминале
# или
npx expo start --android
```

### Физическое устройство:
1. Убедитесь, что устройство и компьютер в одной Wi-Fi сети
2. Отсканируйте QR-код из терминала Metro
3. Или используйте tunnel режим:
   ```bash
   npx expo start --tunnel
   ```

## 🐛 Если приложение не открывается:

### Проверьте логи Metro:
- Откройте терминал где запущен Metro
- Ищите ошибки (красный текст)
- Проверьте, что все модули загружаются

### Проверьте устройство/эмулятор:
- **iOS**: Убедитесь, что Simulator запущен
- **Android**: Проверьте `adb devices` - устройство должно быть в списке

### Проверьте порты:
```bash
# Порт 8081 должен быть занят Metro
lsof -i :8081

# Если порт занят другим процессом, убейте его:
lsof -ti:8081 | xargs kill -9
```

### Диагностика:
```bash
./scripts/check-app-startup.sh
```

## 🔍 Возможные проблемы:

### 1. "Unable to connect to Metro"
**Решение:**
- Проверьте, что Metro запущен
- Проверьте IP адрес (для LAN режима)
- Попробуйте tunnel режим: `npx expo start --tunnel`

### 2. "Network request failed"
**Решение:**
- Убедитесь, что устройство и компьютер в одной сети
- Отключите VPN
- Используйте `--tunnel` режим

### 3. Приложение показывает белый экран
**Решение:**
- Проверьте логи Metro на ошибки
- Проверьте, что `app/splash.tsx` экспортируется правильно
- Убедитесь, что `app/_layout.tsx` правильно настроен

### 4. TypeScript ошибки
**Решение:**
- Ошибки в тестах не влияют на запуск
- Если есть ошибки в основном коде, исправьте их:
  ```bash
  npx tsc --noEmit
  ```

## 📋 Чек-лист перед запуском:

- [ ] Metro bundler запущен на порту 8081
- [ ] Устройство/эмулятор подключен
- [ ] Кэш очищен
- [ ] Все файлы на месте (app/_layout.tsx, app/splash.tsx)
- [ ] Нет критических ошибок в консоли Metro

## 🎯 Быстрая команда для перезапуска:

```bash
killall node && rm -rf .expo .expo-shared && npx expo start --clear
```

