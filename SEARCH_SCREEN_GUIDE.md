# Экран поиска и фильтров

## 🔍 Новый экран поиска (`app/(tabs)/search.tsx`):

### **Основные функции:**
- ✅ **Поиск по тексту** - марка, модель автомобиля
- ✅ **Быстрые фильтры** - популярные марки в горизонтальном скролле
- ✅ **Расширенные фильтры** - модальное окно с полным набором опций
- ✅ **Debounced поиск** - поиск с задержкой 500мс для оптимизации
- ✅ **Результаты в реальном времени** - обновление при изменении фильтров

### **Фильтры:**
- 🚗 **Марка** - 16 популярных брендов
- 💰 **Цена** - 6 диапазонов от "До 500 тыс" до "Более 5 млн"
- 📅 **Год выпуска** - 5 диапазонов от "2020-2025" до "До 2005"
- 🏙️ **Город** - 10 городов Кыргызстана
- ⚙️ **Коробка передач** - Автомат, Механика, Вариатор, Робот

## 🎨 Дизайн:

### **Header:**
- **Заголовок**: "Поиск" (32px, жирный)
- **Разделитель**: Тонкая линия снизу

### **Search Bar:**
- **Поле ввода**: С иконкой поиска и кнопкой очистки
- **Кнопка фильтров**: С бейджем количества активных фильтров
- **Дизайн**: Темная тема с красными акцентами

### **Quick Filters:**
- **Горизонтальный скролл** с популярными марками
- **Chip дизайн** - скругленные кнопки
- **Активное состояние** - красный фон для выбранных

### **Results:**
- **Карточки результатов** с превью, информацией и ценой
- **AI Score** - бейдж с состоянием автомобиля
- **Навигация** - переход к деталям автомобиля

## 🔧 Технические детали:

### **Метод `searchCars` в Supabase:**
```typescript
async searchCars(options: {
  searchQuery?: string;
  brand?: string;
  minPrice?: number;
  maxPrice?: number;
  minYear?: number;
  maxYear?: number;
  location?: string;
  transmission?: string;
  limit?: number;
  offset?: number;
}) {
  // Построение запроса с фильтрами
  // Поддержка текстового поиска
  // Пагинация результатов
}
```

### **Фильтры:**
```typescript
interface Filters {
  searchQuery: string;
  brand: string | null;
  minPrice: number | null;
  maxPrice: number | null;
  minYear: number | null;
  maxYear: number | null;
  location: string | null;
  transmission: string | null;
}
```

### **Debounced поиск:**
```typescript
useEffect(() => {
  const debounce = setTimeout(() => {
    searchCars();
  }, 500);
  
  return () => clearTimeout(debounce);
}, [filters]);
```

## 📱 Пользовательский опыт:

### **Сценарий использования:**
1. **Открывает вкладку Search**
2. **Вводит текст** в поле поиска
3. **Выбирает быстрые фильтры** или открывает расширенные
4. **Видит результаты** в реальном времени
5. **Нажимает на результат** → переход к деталям

### **Состояния:**
- **Загрузка**: Индикатор активности
- **Пустые результаты**: "Ничего не найдено" с кнопкой сброса
- **Начальное состояние**: "Начните поиск"

## 🎯 Функциональность:

### **Поиск по тексту:**
- **Марка**: Toyota, Honda, BMW...
- **Модель**: Camry, Civic, X5...
- **Нечувствительность к регистру**

### **Фильтры:**
- **Множественный выбор** для некоторых категорий
- **Сброс фильтров** - кнопка "Сбросить"
- **Счетчик активных фильтров** в бейдже

### **Результаты:**
- **Карточки с превью** автомобиля
- **Информация**: марка, модель, год, пробег, город
- **AI Score**: состояние автомобиля
- **Цена**: форматированная (тыс/млн)

## 🚀 Готово к тестированию:

1. **Откройте вкладку Search**
2. **Введите текст** в поле поиска
3. **Попробуйте быстрые фильтры** - нажмите на марки
4. **Откройте расширенные фильтры** - кнопка с иконкой
5. **Выберите фильтры** и нажмите "Применить"
6. **Нажмите на результат** - должен открыться детальный экран

**Теперь пользователи могут легко найти нужный автомобиль!** 🎉
