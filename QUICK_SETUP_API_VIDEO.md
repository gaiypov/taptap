# 📹 Установка API Video ключа — Быстрая инструкция

## 🎯 Что нужно сделать

В файле `backend/.env` сейчас стоит placeholder:
```env
API_VIDEO_KEY=your-api-video-key
```

Нужно заменить на реальный ключ.

## 📝 Пошаговая инструкция

### 1. Получите API ключ от api.video

1. Откройте https://dashboard.api.video/
2. Зарегистрируйтесь или войдите
3. Перейдите в раздел **"API Keys"** (в меню слева)
4. Скопируйте ваш API ключ (обычно начинается с `vi...`)

### 2. Откройте файл `backend/.env`

Найдите строку:
```env
API_VIDEO_KEY=your-api-video-key
```

Замените на:
```env
API_VIDEO_KEY=ваш_реальный_ключ_здесь
```

**ВАЖНО:**
- Замените `ваш_реальный_ключ_здесь` на ключ из dashboard
- Не используйте кавычки
- Ключ должен быть на одной строке

### 3. Перезапустите бэкенд

```bash
cd backend
# Остановите текущий процесс (Ctrl+C)
npm run dev
```

## ✅ Проверка

### Автоматическая проверка:
```bash
cd backend
npm run check-env
```

### Тест API:
```bash
curl -X POST http://localhost:3001/api/video/create \
  -H "Content-Type: application/json" \
  -d '{"title":"Test Video"}'
```

**Ожидаемый ответ:**
```json
{
  "success": true,
  "videoId": "vi...",
  "uploadToken": "tok_..."
}
```

## 📖 Подробная инструкция

См. [backend/SETUP_API_VIDEO.md](backend/SETUP_API_VIDEO.md)

