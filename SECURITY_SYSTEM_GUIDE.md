# 🛡️ Система защиты 360Auto

Комплексная система защиты от неподобающего контента и ботов.

---

## 📋 Что включено

### 1. ✅ **Модерация контента (Content Moderation)**
- Автоматическая проверка изображений/видео через Google Cloud Vision API
- Детектирует:
  - 🔞 Порнографию и эротику
  - ⚔️ Насилие
  - 🩺 Медицинский контент
  - 🎭 Спуф/фейк контент
- Автоматическое одобрение/отклонение/модерация

### 2. ⏱️ **Rate Limiting**
- Лимиты на действия пользователей
- Учитывает тип пользователя:
  - **Новые** (0-7 дней): 3 объявления/день
  - **Обычные** (7+ дней, есть продажи): 5 объявлений/день
  - **Верифицированные**: 10 объявлений/день
  - **Дилеры/Автосалоны**: 15 объявлений/день
  - **Премиум**: 50 объявлений/день

### 3. 🎯 **Trust Score**
- Система доверия (0-100)
- Факторы:
  - +15: Телефон верифицирован
  - +5: Есть аватар
  - +10: Дилер (верифицирован)
  - +20: Возраст аккаунта (макс)
  - +30: Успешные продажи (макс)
  - +20: Положительные отзывы (макс)
  - -50: Жалобы пользователей (макс)
- Автоматическое одобрение при Trust Score >= 60
- Обязательная модерация при Trust Score < 30

### 4. 🚩 **Система жалоб**
- Кнопка "Пожаловаться" на объявления, пользователей, сообщения
- 8 типов жалоб:
  - Неподобающий контент
  - Спам
  - Мошенничество
  - Дубликат
  - Вводящая информация
  - Неверная цена
  - Подозрение на угон
  - Другое
- Автоматическое скрытие после 3+ жалоб
- Защита от повторных жалоб

---

## 🚀 Установка

### Шаг 1: Обновить базу данных

Выполните SQL скрипт в Supabase SQL Editor:

```bash
# Откройте файл:
supabase-security-updates.sql

# Скопируйте и вставьте в Supabase SQL Editor
# Нажмите "Run"
```

Это создаст:
- Таблицы: `reports`, `moderation_logs`, `rate_limit_violations`
- Поля в `users`: `is_dealer`, `trust_score`, `dealer_type`
- Индексы для производительности
- RLS политики
- Функции и триггеры

### Шаг 2: Настроить Google Cloud Vision API

1. **Создайте проект в Google Cloud:**
   - Перейдите: https://console.cloud.google.com/
   - Создайте новый проект или выберите существующий

2. **Включите Vision API:**
   - В боковом меню: APIs & Services > Library
   - Найдите: "Cloud Vision API"
   - Нажмите "Enable"

3. **Создайте API ключ:**
   - APIs & Services > Credentials
   - Create Credentials > API Key
   - Скопируйте ключ

4. **Добавьте ключ в `app.json`:**

```json
{
  "expo": {
    "extra": {
      "GOOGLE_VISION_API_KEY": "AIzaSy... ВАШ КЛЮЧ"
    }
  }
}
```

⚠️ **Важно:** Не коммитьте `app.json` с ключами в Git!

### Шаг 3: Установка завершена! ✅

---

## 📖 Использование

### 1. Модерация при загрузке

```typescript
import { moderateImage, moderateVideo } from '@/services/contentModeration';

// Проверить изображение
const result = await moderateImage(imageBase64);

if (!result.isApproved && !result.needsReview) {
  // Отклонено автоматически
  alert('Контент нарушает правила платформы');
} else if (result.needsReview) {
  // Отправить на ручную модерацию
  console.log('Требуется проверка модератора');
} else {
  // Одобрено
  console.log('Контент прошел проверку');
}
```

### 2. Проверка лимитов

```typescript
import { checkUploadLimit } from '@/services/rateLimiting';

const limitCheck = await checkUploadLimit(userId);

if (!limitCheck.allowed) {
  alert(`Превышен лимит загрузок (${limitCheck.limit}/день). Попробуйте завтра.`);
  return;
}

// Продолжить загрузку
console.log(`Осталось загрузок сегодня: ${limitCheck.remaining}`);
```

### 3. Расчет Trust Score

```typescript
import { calculateTrustScore, getTrustLevel } from '@/services/trustScore';

const factors = await calculateTrustScore(userId);
const level = getTrustLevel(factors.totalScore);

console.log(`Trust Score: ${factors.totalScore}/100 (${level})`);
console.log('Факторы:', factors);
```

### 4. Кнопка жалобы

```tsx
import { ReportButton } from '@/components/ReportButton';

// В экране деталей автомобиля
<ReportButton
  targetType="car"
  targetId={carId}
  reportedUserId={sellerId}
  iconColor="#FFF"
/>
```

---

## 🎯 Типы пользователей (Tiers)

| Тип | Критерии | Лимит объявлений |
|-----|----------|------------------|
| **new_user** | 0-7 дней | 3/день |
| **regular_user** | 7+ дней, есть продажи | 5/день |
| **verified_user** | Верифицирован, 30+ дней | 10/день |
| **dealer** | Дилер/автосалон (is_dealer = true) | 15/день |
| **premium_user** | Премиум подписка | 50/день |

---

## 📊 Мониторинг

### Представления для статистики:

```sql
-- Статистика модерации
SELECT * FROM moderation_statistics
WHERE date >= CURRENT_DATE - INTERVAL '7 days'
ORDER BY date DESC;

-- Статистика жалоб
SELECT * FROM report_statistics
WHERE date >= CURRENT_DATE - INTERVAL '7 days'
ORDER BY date DESC, report_count DESC;
```

### Функции:

```sql
-- Пересчитать Trust Score для пользователя
SELECT update_trust_score('user-uuid-here');

-- Массовое обновление для всех
UPDATE users SET trust_score = update_trust_score(id);
```

---

## 🔒 Безопасность

### RLS Политики:

- ✅ Пользователи могут создавать только свои жалобы
- ✅ Пользователи видят только свои жалобы
- ✅ Логи модерации доступны только через service role
- ✅ Rate limit violations - только для системы

### Автоматические действия:

- **3+ жалобы** → Автоматическое скрытие контента
- **Trust Score < 30** → Обязательная ручная модерация
- **Trust Score >= 60** → Автоматическое одобрение
- **Adult content > 0.6** → Автоматическое отклонение

---

## 💡 Советы

### Для дилеров:

1. Укажите `is_dealer = true` в профиле
2. Заполните `company_name` и `company_license`
3. Получите верификацию (is_verified = true)
4. Лимит автоматически повысится до 15 объявлений/день

### Для улучшения Trust Score:

1. ✅ Верифицируйте телефон (+15)
2. ✅ Добавьте аватар (+5)
3. ✅ Совершайте успешные продажи (+3 за каждую, макс +30)
4. ✅ Получайте положительные отзывы (+20 при рейтинге 4.5+)
5. ❌ Избегайте жалоб (-10 за каждую)

---

## 🆘 Troubleshooting

### Google Vision API не работает

**Проблема:** Ошибка при модерации
**Решение:**
1. Проверьте, что API ключ добавлен в `app.json`
2. Убедитесь, что Vision API включен в Google Cloud
3. Проверьте квоты (1000 запросов/день бесплатно)

### Лимиты слишком строгие

**Проблема:** Пользователи жалуются на лимиты
**Решение:**
1. Измените значения в `services/rateLimiting.ts`
2. Или повысьте tier пользователя (is_dealer, премиум)

### Ложные срабатывания модерации

**Проблема:** Нормальные фото отклоняются
**Решение:**
1. Понизьте пороги в `services/contentModeration.ts`:
   ```typescript
   // Было: scores.adult >= 4
   // Стало: scores.adult >= 5 (строже)
   ```

---

## 📚 API Reference

### contentModeration.ts

- `moderateImage(imageBase64: string): Promise<ModerationResult>`
- `moderateVideo(videoFrames: string[]): Promise<ModerationResult>`
- `extractKeyFrames(videoUri: string, count: number): Promise<string[]>`

### rateLimiting.ts

- `getUserTier(userId: string): Promise<UserTier>`
- `checkUploadLimit(userId: string): Promise<RateLimitCheck>`
- `checkMessageLimit(userId: string): Promise<boolean>`
- `checkLikeLimit(userId: string): Promise<boolean>`
- `getUserLimits(userId: string): Promise<RateLimitConfig>`

### trustScore.ts

- `calculateTrustScore(userId: string): Promise<TrustScoreFactors>`
- `getTrustLevel(score: number): 'low' | 'medium' | 'high'`
- `shouldAutoApproveListing(score: number): boolean`
- `requireManualReview(score: number): boolean`
- `updateUserTrustScore(userId: string): Promise<number>`

---

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте логи в консоли
2. Проверьте Supabase Dashboard > Logs
3. Проверьте Google Cloud Console > Logs

---

**Система защиты готова к работе! 🎉**

