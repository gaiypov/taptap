# 🚨 КРИТИЧЕСКАЯ ПРОБЛЕМА БЕЗОПАСНОСТИ

## ⚠️ СЕКРЕТЫ УЖЕ В GITHUB!

**Дата обнаружения:** Январь 2025  
**Статус:** 🔴 КРИТИЧНО - Требуется немедленное действие

---

## 🔴 ПРОБЛЕМА

В файле `app.json` находятся секретные ключи, которые **УЖЕ ЗАКОММИТИРОВАНЫ** в GitHub:

```json
"SUPABASE_SERVICE_ROLE_KEY": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
"GOOGLE_VISION_API_KEY": "AIzaSyCDq7xTy4yrPvBr5JjGNUEXaXZ70fVyJGg",
"EXPO_PUBLIC_SMS_PASSWORD": "83fb772ee0799a422cce18ffd5f497b9",
"EXPO_PUBLIC_APIVIDEO_API_KEY": "OhnRGcRvd7YS7H7TV6uwXRNgLvocjuAfGfR2qAebSKv"
```

**Репозиторий:** https://github.com/gaiypov/360auto_kg.git

---

## ⚡ НЕМЕДЛЕННЫЕ ДЕЙСТВИЯ (СЕЙЧАС!)

### Шаг 1: РОТАЦИЯ ВСЕХ КЛЮЧЕЙ (Приоритет #1)

#### 1.1 Supabase Service Role Key
- [ ] Зайти в Supabase Dashboard → Settings → API
- [ ] Сгенерировать новый Service Role Key
- [ ] Обновить в `.env` файле
- [ ] **СТАРЫЙ КЛЮЧ УЖЕ СКОМПРОМЕТИРОВАН**

#### 1.2 Google Vision API Key
- [ ] Зайти в Google Cloud Console → APIs & Services → Credentials
- [ ] Удалить или ограничить старый ключ
- [ ] Создать новый API ключ
- [ ] Обновить в `.env` файле

#### 1.3 SMS API Credentials
- [ ] Зайти в панель smspro.nikita.kg
- [ ] Изменить пароль
- [ ] Обновить в `.env` файле

#### 1.4 api.video API Key
- [ ] Зайти в api.video dashboard
- [ ] Сгенерировать новый API key
- [ ] Обновить в `.env` файле

---

### Шаг 2: УДАЛИТЬ СЕКРЕТЫ ИЗ app.json

#### 2.1 Создать `.env` файл (если нет)

```bash
# .env (НЕ КОММИТИТЬ!)
SUPABASE_URL=https://thqlfkngyipdscckbhor.supabase.co
SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
SUPABASE_SERVICE_ROLE_KEY=<новый_ключ>
GOOGLE_VISION_API_KEY=<новый_ключ>
EXPO_PUBLIC_SMS_LOGIN=superapp
EXPO_PUBLIC_SMS_PASSWORD=<новый_пароль>
EXPO_PUBLIC_SMS_API_URL=https://smspro.nikita.kg/api/message
EXPO_PUBLIC_SMS_SENDER=bat-bat.kg
EXPO_PUBLIC_APIVIDEO_API_KEY=<новый_ключ>
```

#### 2.2 Обновить app.json

Удалить секреты, оставить только публичные конфиги:

```json
{
  "expo": {
    "extra": {
      "apiUrl": "http://192.168.1.16:3001/api",
      "aiMode": "development",
      "eas": {
        "projectId": "your-project-id"
      },
      "EXPO_PUBLIC_AI_MODE": "development",
      "EXPO_PUBLIC_USE_MOCK": "false",
      "EXPO_PUBLIC_SUPABASE_URL": "https://thqlfkngyipdscckbhor.supabase.co",
      "EXPO_PUBLIC_SUPABASE_ANON_KEY": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
      // УДАЛИТЬ ВСЕ ОСТАЛЬНЫЕ СЕКРЕТЫ!
    }
  }
}
```

#### 2.3 Использовать expo-constants для env переменных

В коде использовать:

```typescript
import Constants from 'expo-constants';

const supabaseServiceKey = process.env.SUPABASE_SERVICE_ROLE_KEY || 
  Constants.expoConfig?.extra?.SUPABASE_SERVICE_ROLE_KEY;
```

---

### Шаг 3: ОЧИСТИТЬ ИСТОРИЮ GIT (ОПЦИОНАЛЬНО, но рекомендуется)

**ВНИМАНИЕ:** Это удалит секреты из истории Git, но они уже могли быть скопированы.

#### 3.1 Использовать git-filter-repo (рекомендуется)

```bash
# Установить git-filter-repo
pip install git-filter-repo

# Удалить app.json из истории
git filter-repo --path app.json --invert-paths

# Или заменить секреты в истории
git filter-repo --replace-text <(echo "SUPABASE_SERVICE_ROLE_KEY==>SUPABASE_SERVICE_ROLE_KEY=<REDACTED>")
```

#### 3.2 Или использовать BFG Repo-Cleaner

```bash
# Скачать BFG
# https://rtyley.github.io/bfg-repo-cleaner/

# Очистить секреты
java -jar bfg.jar --replace-text secrets.txt
```

#### 3.3 Force push (ОСТОРОЖНО!)

```bash
# После очистки истории
git push origin --force --all
git push origin --force --tags
```

**⚠️ ВНИМАНИЕ:** Force push перезапишет историю. Убедитесь, что все в команде синхронизированы.

---

### Шаг 4: ПРОВЕРИТЬ .gitignore

Убедиться, что `.env` файлы игнорируются:

```gitignore
.env
.env.local
.env.*.local
```

---

### Шаг 5: ОБНОВИТЬ .env.example

Создать шаблон без секретов:

```bash
# .env.example
SUPABASE_URL=your_supabase_url
SUPABASE_ANON_KEY=your_anon_key
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key
GOOGLE_VISION_API_KEY=your_google_key
EXPO_PUBLIC_SMS_LOGIN=your_sms_login
EXPO_PUBLIC_SMS_PASSWORD=your_sms_password
EXPO_PUBLIC_APIVIDEO_API_KEY=your_apivideo_key
```

---

## 📋 ЧЕКЛИСТ ИСПРАВЛЕНИЯ

- [ ] Ротировать Supabase Service Role Key
- [ ] Ротировать Google Vision API Key
- [ ] Ротировать SMS пароль
- [ ] Ротировать api.video ключ
- [ ] Удалить секреты из app.json
- [ ] Создать .env файл
- [ ] Обновить .env.example
- [ ] Проверить .gitignore
- [ ] Очистить историю Git (опционально)
- [ ] Закоммитить исправления
- [ ] Проверить, что секреты не попали в новый коммит

---

## 🔒 ДОПОЛНИТЕЛЬНЫЕ МЕРЫ БЕЗОПАСНОСТИ

### 1. GitHub Secrets (для CI/CD)

Если используете GitHub Actions, добавьте секреты в:
- Settings → Secrets and variables → Actions

### 2. Мониторинг

- [ ] Настроить алерты на использование старых ключей
- [ ] Проверить логи на подозрительную активность
- [ ] Мониторить расходы API (внезапный рост = возможный взлом)

### 3. Ограничения API ключей

- [ ] Ограничить Google Vision API по IP/домену
- [ ] Установить квоты на api.video
- [ ] Настроить rate limiting на SMS API

---

## ⏱️ ВРЕМЯ НА ИСПРАВЛЕНИЕ

- **Ротация ключей:** 30 минут
- **Удаление из app.json:** 15 минут
- **Очистка истории Git:** 1 час (опционально)
- **Тестирование:** 30 минут

**ИТОГО:** 2-3 часа

---

## 🚨 КРИТИЧНОСТЬ

**Уровень:** 🔴🔴🔴 КРИТИЧЕСКИЙ

**Риски:**
- Полный доступ к базе данных (Service Role Key)
- Несанкционированное использование API (расходы)
- Утечка пользовательских данных
- Нарушение GDPR/законов о защите данных

**Действие:** НЕМЕДЛЕННО (сегодня!)

---

## 📞 КОНТАКТЫ

Если нужна помощь:
- Backend Lead: для ротации ключей
- DevOps: для настройки env переменных
- Security: для аудита

---

**Статус:** 🔴 ТРЕБУЕТСЯ НЕМЕДЛЕННОЕ ДЕЙСТВИЕ

